# ABF + Nora — Product Backlog
Last updated: Aug 6, 2026 (post-audit fix pass — /timeline retirement, Memory Test unlock notification, ENGAGEMENT_PATTERN checkin signal, Wednesday/Thursday cutoff bugs, 4 Flirt postcard bugs)

This file is permanent and cumulative. Add items, update status, never delete history.

---

## STATUS KEY
- 🔴 Blocked
- 🟡 Designed, not built
- 🟢 Ready to build
- ✅ Done
- 📋 Backlog

---

## ABF — FEATURES

### Date Night
- ✅ Planning flow redesign — itinerary-first, stops visible as cards while building, not hidden in bottom bar
- ✅ Photo upload in date detail — multi-select, uploads all chosen files
- 🟢 Home page upcoming strip — show planned date within 48 hours contextually, disappears otherwise. Agreed design: one line between Nora card and Flirt card, only within 48hr window
- ✅ Movie/Show stop map fix — non-location stops cause map to disappear
- ✅ List/Map toggle — removed dead toggle in custom/page.js (edit/page.js's toggle was already functional, untouched)
- ✅ Expanding map hides category pills — resolved via the planning-flow redesign above
- ✅ Ideas For You Two personalization — built differently than originally scoped: instead of passing Nora's free-text couple knowledge into a search prompt, wired selectDateSuggestions() to weight each vibe's category by the couple's relationship_assessments scores, exclude previously-visited places (avoidPlaceIds), and apply real price-level filtering. Ticketmaster events now vary per vibe via a keyword param (adventure -> "experience", culture -> "theater", nightlife -> "music"). Passing actual Nora couple-knowledge into a live web-search prompt is still the bigger unbuilt idea — see AI web-search agent below.
- ✅ Post-date reflection mechanic (new item, not originally listed) — replaced the 5-star "How was the date?" modal with 4 text reaction pills (Loved it / Really good / It was fine / Not for us — no emoji, ABF rule), optional note, per-user Mark as Done guard (was repeat-clickable before), immediate push to the partner when one side completes, Nora observation once both have reflected, and a once-daily cron nudge for whoever hasn't reflected on a past date yet.
- 🟡 During-date interactivity — location-based prompts, Game Room integration at stops, photo capture prompt
- 📋 Game Room + Date Night integration — promote Game Room within date flow, between stops
- 🟡 AI web-search suggestion agent — discussed in depth July 27-28. Cost estimate ~$0.05-0.08/request ($25-3000/month at 100-10k users, Sonnet 5 pricing + $0.01/search). Explicit sequencing agreed: wire up the existing engine first (done above), then decide if it's still needed. Verdict as of July 28: Ticketmaster keyword variation is a band-aid, not a real fix — it can't surface non-Ticketmaster experiences (the "Mind of a Serial Killer" example) no matter how it's tuned. Still holding off building until real usage data comes in on the wired-up version.

### Audit findings — Aug 3 (Date Night feature audit + fix sprint)
- ✅ Full audit of Date Night end to end — every API route, the custom-date create/edit/detail pages, the suggestion engine, and the hero-photo/reaction helpers. Distinct from every other feature audited this engagement: custom-date create/edit/detail don't go through API routes at all — they write directly from the browser to Supabase, so authorization there lives entirely in Postgres RLS policies rather than `requireUser`/`verifyCoupleMembership`. Verified the actual policy SQL (not just policy names) with Matt via the Supabase SQL Editor before concluding anything — `pg_policies` showed SELECT/UPDATE both correctly scope to couple membership via an EXISTS against `couples`; INSERT was the one real gap.
- ✅ CRITICAL, applied by Matt Aug 4 — `docs/database/custom-dates-insert-policy-fix.sql`: the `custom_dates` INSERT policy only checked `auth.uid() = user_id`, never that `couple_id` actually belongs to the caller (unlike SELECT/UPDATE, which both do). An attacker could insert a row with their own user_id but a stranger couple's couple_id — SELECT/UPDATE's couple-scoping would then make the planted row visible (and editable) to the real couple, but the delete flow (keyed on the row's own user_id/shared_with fields, not couple_id) means the victims couldn't remove it themselves. Fix adds the same couple-membership EXISTS check to INSERT's WITH CHECK that SELECT/UPDATE already have, while still allowing couple_id IS NULL. Run in the Supabase SQL Editor and verified live via `pg_policies` — the INSERT row's `with_check` now shows the EXISTS clause.
- ✅ Confused-deputy fix, commit 098df46: `dates/conversation-starters` verified the caller belongs to the supplied `coupleId` but then updated `custom_dates` by a separately-supplied `dateId` with no check that the date actually belonged to that couple — same bug shape already fixed twice this engagement (`follow-through/report`, `ritual/update`). Added `.eq('couple_id', coupleId)` to the update.
- ✅ Cost-abuse fix, commit 098df46: `dates/suggestions` (GET+POST) was the one Date Night route with zero auth at all, unlike its sibling `ticketmaster` route which requires a session for the same class of external-API proxy — left open, anyone (logged in or not) could hammer the Google Places API key through it for free. Added `requireUser`. POST also had an unused service-role Supabase client, created but never referenced; removed. Client callers (`fetchDateSuggestions`/`fetchNearbyPlaces` in `lib/date-suggestions.js`) sent no Authorization header at all before this — updated both, otherwise this fix would have silently broken "Ideas For You Two" entirely.
- ✅ `getHeroPhoto()` fixed, commit 02f1960 (`lib/date-night.js`): two bugs. (1) Docstring claimed it prefers real-location photos over custom/manual stops; the code actually concatenated locationPhotos + otherPhotos into one array and picked proportionally — 1 location photo + 3 custom-stop photos meant a 75% chance the hero was a custom stop, the opposite of the stated intent. Now picks only from locationPhotos when any exist. (2) Media stops (movies/shows) were miscategorized as location photos — the old check only excluded `custom-` prefixed place_ids, not `media-` ones. Both fixed.
- ✅ Dead code cleanup, commit 02f1960 (`lib/date-suggestions.js`): removed an unused `weakAreas` variable and an unreachable fallback branch (`Object.keys(DATE_CATEGORIES)` always has entries, so the "no assessment data" fallback could never trigger). Documented, rather than silently left, why the function's full category ranking — and the fully-built but never-triggered weather-adjustment logic inside it — has no consumer: `app/dates/page.js`'s real category picker (`pickCategoryForVibe`) was built later specifically to fix the "Something Slow / Something Just Us collapsing onto the same category" bug (already logged above) and never went back to wire this in. Left the weather logic in place rather than deleting a working feature — whether to revive it is a product call.
- ✅ Delete dead-end fixed, commit 35dee5e: a custom date created but never explicitly "shared" with a partner (`shared_with` stays null until the creator taps "Ready to share?") could never actually be deleted — `/delete/request` set `pending_delete`, but `/delete/confirm` requires someone OTHER than the requester, and no one else ever passes that route's ownership check when `shared_with` is null. The creator could request deletion and then only ever cancel it back, never complete it. Fixed by having `/delete/request` delete immediately when the date was never shared, skipping the two-party flow entirely (a solo date doesn't need a partner's sign-off to remove). Updated both client callers (`app/dates/[id]/page.js`, `app/dates/[id]/edit/page.js`) to handle the new immediate-delete response.
- ✅ Date History filtering fixed, commit 3730aab: the hub page's "Date History" preview had no past-vs-future filter at all — the same date shown in "Next Up" could also render again in "Date History" below it, and a section titled "history" could show dates that hadn't happened yet. Now excludes the upcoming date's id and filters to completed/past dates only. Also aligned `custom_dates`/`date_plans` query ordering (was `created_at` vs `date_time`, inconsistent though masked by a client-side re-sort) and added the `date_plans` status exclusion (`cancelled`) that `custom_dates` already had (`pending_delete`) — a cancelled date_plans row would previously still show up in history. Removed a dead unused `now` variable in both `dates/page.js` and `dates/history/page.js`.
- 🟢 lint-clean on every touched file (`dates/suggestions`, `dates/conversation-starters`, `lib/date-suggestions.js`, `lib/date-night.js`, `dates/delete/request`, `app/dates/[id]/page.js`, `app/dates/[id]/edit/page.js`, `app/dates/page.js`, `app/dates/history/page.js`) — only pre-existing errors remain (react/no-unescaped-entities in `[id]/page.js`, a set-state-in-effect warning in `history/page.js`, missing-dependency warnings in two useEffects, img-element warnings), all confirmed via diff to be untouched by this sprint's changes.
- 🟢 Every client caller checked against its route's new/changed contract before committing — no request/response shape changes anywhere except the two intentional ones (`dates/suggestions` needing an Authorization header now, `/delete/request`'s new `deleted` response flag), both of which had their callers updated in the same commit.
- 📋 Not yet live-tested end to end — real confirmation happens when Matt & Cass exercise "Ideas For You Two", plan/edit/delete a custom date (shared and unshared), and browse Date History live. The INSERT RLS migration is applied and verified live; this remaining item is just exercising the actual UI flows.

### Couples Memory
- 🟢 Dream trip flow on /us/add — currently stubs to /shared/add, needs proper creation flow
- 📋 Photo attribution in memory events — subtle indicator of who added which photo. Not vital, tabled.
- ✅ Assessment data now actually reaches Nora's memory, Aug 5 — was silently dropped for every couple until today (two layered bugs, see "Root-cause fix — Aug 5" under ABF — TECHNICAL for the full trace). Fixed going forward for all new couples; manually backfilled for Matt & Cass's own real assessment data.

### Daily Rhythm
- ✅ "The Follow-Through" (working name) — post-activity real-world action mechanic, built July 29. Design settled July 28-29 after a full research + competitor pass and state-machine stress test — see Sessions/NOW_DO_THIS_DESIGN.md and Sessions/FOLLOW_THROUGH_GENERATION_SPEC.md. Bet-only v1: per-partner Nora-generated action (tagged other-directed or self-directed at generation time, determines what Tier 2 shows), two-tier reveal (solo reaction on your own report, mutual synthesis once both report), distress gate (trajectory check + per-night classifier), rare wildcards (bigger-scope or partner-authored, ~10% eligible nights), asymmetric-completion nudge reusing the existing Nora hero-card slot. No new dashboard module — reuses Bet's own card slot, morphing between a carryover report face and today's activity via a flip transition. Not yet live-tested end to end (next Tuesday's Bet, or a forced `?bet=true`, is the first real chance). Weekly Reflection visual element (the recap row) not yet built — separate follow-up.
- ✅ Follow-Through extended to Spark (July 30) — built per Sessions/FOLLOW_THROUGH_WEEKLY_ROLLOUT.md. Added spark_responses.reveal_seen_at (set automatically once Spark's pillsShown animation completes, mirroring Bet's tap-based flag). Extracted the Bet-only generateFollowThrough() into lib/follow-through.js, generalized over source type/label/question/answers, so both routes share one implementation. FollowThroughCard now wraps SparkCard the same way it wraps BetCard.
- ✅ Follow-Through weekly rollout completed (July 30) — Wednesday/Notice and Thursday both wired in, after actually reading app/api/cron/scheduled-tasks/route.js and vercel.json rather than guessing. Confirmed: both reveal on fixed dedicated cron entries (not a poller), and both already reveal "either way" (zero, one, or two responses) — Follow-Through generation is gated to only fire when both sides actually responded, called from inside processWednesdayReveal/processWednesdayCutoff/processThursdayReveal themselves since nothing here is triggered by a user action the way Bet/Spark's respond routes are. Wednesday's data (one shared implicit question, two notices) mapped directly onto the existing generateFollowThrough. Thursday's didn't — each partner gets a different individualized observation+question — so generateFollowThrough gained optional myQuestion/theirQuestion params (backward compatible; existing callers unaffected). hasSeenSourceReveal needs no per-user gate for either: unlike Bet/Spark's tap-through/auto-play reveals, Wednesday/Thursday's reveal is just a shared cron-driven status flip both partners see identically. This closes out the full weekly rollout — Bet, Spark, Wednesday, Thursday all have Follow-Through; Friday/Ritual remains intentionally exempt (has its own equivalent via the Ritual enrichment work instead).
- ✅ Real bug found and fixed during the Spark extension: /api/follow-through/today never gated on the user's own source-activity reveal (bet_responses.reveal_seen_at / spark_responses.reveal_seen_at) before going active — meaning Follow-Through could steal the card slot the instant generation fired, before either partner had even seen their own Bet/Spark reveal. This was explicitly called for in NOW_DO_THIS_DESIGN.md's "Per-user gating" section but never actually wired in. Fixed via hasSeenSourceReveal() in the /today route.
- ✅ Ritual enrichment built (July 30) — all 4 pieces from Sessions/RITUAL_ENRICHMENT_DESIGN.md. (1) Deleted the static NORA_WEEK_MESSAGES lookup table, replaced with a live noraReact call at check-in time referencing the actual ritual + streak, persisted to ritual_completions.nora_reaction. (2) Added an optional, skippable reflection textarea at check-in (ritual_completions.reflection_note), feeding the same generation call. (3) Added an occasional (~6-week dormancy gate, ~25% roll) Nora "still going?" revisit of adopted rituals, surfaced in the Library view's suggestion slot (rituals.last_revisited_at, rituals.pending_revisit_message) — "Still going" resets the dormancy clock, "We drifted from this one" retires the ritual through the same two-person confirm transition adopting it required. (4) Partner-loop nudge: whoever didn't personally check in gets a one-time nudge via the same hero-card slot Follow-Through's nudge uses (ritual_completions.partner_notified, .partner_note), with a genuine lightweight capture point (a small skippable textarea on /dashboard, reached via a URL param, no new screen).
- ✅ Real bug found and fixed during the Ritual enrichment build — discovering-phase "Not for us" (handleRetire) never persisted status='retired' anywhere; it only logged a checkin and set the status on local client state, so reloading the dashboard silently un-retired the ritual. Fixed by adding a guarded `retire` flag to /api/ritual/checkin (only applies from 'discovering' status, so it can never bypass the two-person confirm an ADOPTED ritual's retire requires). Caught while tracing the exact transition the new revisit mechanic's "We drifted from this one" needed to reuse.
- ✅ Extracted the two-person retire request/confirm logic (previously only in /api/ritual/retire) into lib/ritual-retire.js so the new revisit mechanic's "drifted" response could call the identical transition instead of duplicating it.
- ✅ Bet flip animation only triggerable by whoever answers last (July 28) — root cause: the reveal-skip shortcut was gated on the couple both having answered, not on whether this specific user had personally triggered their own reveal yet. Whoever answers first has nothing to do but leave and come back, so they'd reopen to an already-resolved Bet and get skipped straight past the flip animation, while whoever answered last (still on the live page) got the full experience. Fixed via bet_responses.reveal_seen_at, set per-user the first time they tap "Reveal the cards" — needs a migration, see chat.
- 📋 Past Reflections collapsed list design
- 📋 Sunday Review outcome tracking
- 📋 Game Room Talk to Nora links — verify exist, add if missing

### Game Room
- ✅ Memory Test unlock gate + question-generator data source fixed July 31, commits 218a24b, 85d1ce0, 38a1a04 — full writeup below under Audit findings. Restored the original design (real per-couple thresholds, not the "temp: unlock for testing" zeroed values that never got reverted), enforced server-side in confirm-type (not just UI hiding), wired the dynamic tile state into the game-room home page (was hardcoded unavailable for everyone), and fixed the memory question generator itself, which was reading Spark/Bet history from two tables (today_responses, bets.user1_answer/user2_answer) nothing in the live app writes to anymore — it now reads from spark_responses/bet_responses like the rest of the app does.
- ✅ Signals audit complete + Dates/Flirts wired in, July 31, commit 2603e52. "Been" = Timeline (Us section's Been/Now/Ahead structure), already directly pulled — no gap there. Full findings and the AI-coach privacy issue found along the way are under Audit findings below.
- ✅ Full Game Room security sprint complete Aug 4 — found and fixed the most severe vulnerability of the entire engagement (couples-table account takeover), plus a missing-auth route, a confused-deputy write bug, and 8 more of the same shape across every sub-game, plus 4 cross-couple read-leak routes. Full writeup below under Audit findings.

### Session-Based Conversations (ABF)
- 🟡 Session-based Nora conversations for couples — designed (SESSION-DESIGN.md), not built. Nora Standalone has it. ABF needs same pattern.

---

## ABF — TECHNICAL

### Security
- ✅ BOLA (Broken Object Level Authorization) fix — full rollout complete July 31 (started July 30). Full re-audit of all 116 API routes (corrected count, superseding the July 28 estimate of 57/109) found 72 routes with no session-validation pattern at all; 1 excluded as a legitimate OAuth flow (spotify/callback); 71 real. Fix pattern: shared lib/api-auth.js (requireUser + verifyCoupleMembership + getOwnCoupleId), applied route-by-route with its client caller in the same commit — see Audit findings below for exactly which are done.
  - Tier 1 (Bet, Ritual, Follow-Through, Dashboard — 16 routes): ✅ done, commits 17be79c, d2cd5d1, 0202b57, 1b0f8fa.
  - Tier "also sensitive" (Ahead: 2 routes, us/save-flirt, spark/reveal, nora-conversation — 5 routes): ✅ done, commit bb8a30a. ahead/complete and ahead/nora-line now derive couple_id from the shared_items row itself rather than a client-supplied value; us/save-flirt and spark/reveal derive it from their respective flirt/spark row; nora-conversation has no couple-scoped resource so only requireUser was added (closes unauthenticated LLM-cost abuse, not classic BOLA).
  - Game Room (35 routes across 5 sub-games): ✅ done. The Call (6 routes) commit ec4aead; The Challenge (14 routes) commit ff327f5; The Hunt (5 routes) commit 375e930; Hot Take (3 routes) commit a45b63b; shared lobby/session layer (enter-lobby, round-ready, save-interests, start-session, generate-debrief, generate-hole, nora-nudge — 7 routes) commit c7edc34. Several routes derive couple_id from the resource's own DB row (call_rounds, challenge_rounds, hunt_sessions, hot_take_sessions) rather than trusting a client-supplied coupleId, closing confused-deputy gaps. During The Challenge, briefly mixed up memory/ready and memory/submit route contents mid-edit — caught via lint + git show before committing, both fixed correctly; disclosed to Matt.
  - Lower-stakes tier (assessment, dates, flirts, trips/wander): ✅ done, commit c3875ba. Re-audited each route's actual code rather than assuming the original 9-route estimate — found 13 genuinely vulnerable routes plus several that were already safe (see below), a wider spread than the July 30 estimate. Fixed: assessment/insight, assessment/personal-summary (requireUser only — LLM-cost abuse gate, no DB object to scope), assessment/seed-memory (derives userId from token, verifies coupleId membership before trusting it for memory writes), dates/complete (was already token-checked but trusted a client-supplied userId to decide which partner's reaction to write — classic confused-deputy; now derives from token and 403s if the caller isn't actually in that couple), dates/conversation-starters (was token-checked but trusted client coupleId with zero membership check, exposing another couple's check-ins/assessments/reflections — now verified), flirts/check-profile (had no auth at all, GET with a raw userId query param — now derives from token), flirts/generate (no auth, trusted client userId/partnerId — partner is now derived from the couple record server-side so a flirt can't be generated toward someone who isn't actually your partner), flirts/mark-sent, flirts/mark-viewed, flirts/save-profile (all had zero auth, trusted a client-supplied userId for the write), flirts/send, flirts/inbox (added couple-membership verification for defense-in-depth, though both were already implicitly self-scoped by receiver_id/sender_id = user.id), trips/wander (no auth at all, leaked couple names/hobbies/preferences for any guessed coupleId).
  - Already safe, no changes needed: dates/delete/cancel, dates/delete/confirm, dates/delete/request, dates/photos/add (all already verify the caller against the date's own user_id/shared_with/couple), flirts/open, flirts/react (already scope to receiver_id = user.id and derive couple_id from the flirt row itself), dates/suggestions (Google Places proxy, no user data returned — same category as weather).
  - weather left unauthenticated intentionally — no user data returned.
  - This closes the full 71-route BOLA remediation (65 real fixes + verification that the remaining were already safe or intentionally public).
- ✅ 3 of the 4 orphaned dead-code routes deleted July 31, commit 687d560 — bet/lock, flirts/unread, game-room/lobby-status. Before deleting, verified zero callers across all 296 tracked files (not just app/components/) and confirmed each was cleanly superseded by live code: bet/lock → bet_responses per-user-row model in bet/respond; flirts/unread → flirts/inbox's unopened list + FlirtCard.js's opened_at tracking; game-room/lobby-status → enter-lobby's bothInLobby response + direct client-side polling of game_sessions. flirts/mark-viewed also deleted July 31, commit d0e177c, once flirts/unread's removal confirmed nothing reads its viewed_at column anymore — dropped the now-dead fetch call from FlirtSheet.js's receivedFlirt effect first, then removed the route.
- 🔴 game-room/challenge/start — 4th orphaned route, deliberately NOT deleted yet. Also confirmed dead code (zero callers) and its "confirm-type" successor is live, but the route also used to gate Memory Test mode behind an eligibility check (minimum timeline events + Bet/Spark response count + minimum account age via lib/challenge-prompts.js's MEMORY_UNLOCK). That gate has no live replacement — Memory Test is currently a fully open top-level tile on the game-room index page with zero eligibility check, and MEMORY_LOCKED_COPY (imported in app/game-room/lobby/page.js for what looks like a "locked" state) is never actually referenced/rendered anywhere. Needs a dedicated session to decide: rebuild the eligibility gate, deliberately drop it as a design decision, or something else — before this route gets deleted.

### Data
- 🟢 Bet questions category field — 120 questions in lib/bet-questions.js need { category } added (preferences/likely/reactions/confessions). question_category column exists in bets table and is nullable.

### Audit findings — July 28 code review
- ✅ Daily Check-in feature confirmed intentionally killed (July 28) — not a bug. Spark, Bet, etc. are the actual daily-activity engagement surface; Daily Check-in was deliberately retired. Still open: lib/checkin-questions.js is fully orphaned (zero imports) and lib/checkin-patterns.js's analyzeUserPatterns() (used by the AI coach) still reads from the now-dead daily_checkins table. Cleanup pass needed: delete lib/checkin-questions.js, decide whether analyzeUserPatterns should be retired too or repointed at Spark/Bet response data instead.
- 🟢 Morning-after reflection push (see Post-date reflection mechanic above) was gated on a local hour with no corresponding cron schedule entry — fixed July 28, added a dedicated vercel.json cron. Flag: app now has 7 cron entries on one route; Vercel Hobby plan caps at 2, worth confirming plan/dashboard state.
- 🟢 maxPrice/price_level accepted by app/api/dates/suggestions/route.js but never applied to the Google Places call — fixed July 28.
- 📋 ~25 exported functions/constants in lib/ have zero imports anywhere else in the app (e.g. lib/checkin-patterns.js's analyzeCouplePatterns/detectConcerns/generateWeeklySummary, lib/nora-knowledge.js's getDiscrepancyNotes, lib/ritual-suggestions.js's getRitualsByTag/getRitualsByTier/getSeasonalRituals, lib/date-suggestions.js's fetchNearbyPlaces). Some are likely intentional (raw data arrays consumed internally by a wrapper function in the same file); a few look like whole built-but-never-wired capabilities worth a closer look, particularly the checkin-patterns trio and getDiscrepancyNotes. Not verified individually — worth a dedicated pass, not urgent.
- 📋 Known low-priority edge cases documented in code comments, not fixed: a narrow race if both partners tap Mark as Done within the same instant (app/api/dates/complete/route.js), and unavoidable category overlap between two vibes' assessment-driven alternates (app/dates/page.js VIBE_CATEGORIES) when only 5 categories exist for 3 vibes.

### Audit findings — July 29 code review (Follow-Through sprint)
- 🟢 partnerId accepted by generateFollowThrough() in app/api/bet/respond/route.js but never referenced in the function body — caught before commit, removed from both the call site and signature.
- 🟢 Duplicate prefix/status computation in app/api/follow-through/today/route.js (myPrefixEarly/myStatusEarly recomputed the same values as myPrefix/mine.status a few lines later) — introduced during a mid-build fix, cleaned up same session before the sprint closed.
- 📋 5 pre-existing react/no-unescaped-entities lint errors in app/dashboard/page.js (lines ~415, 435, 603, 632, 713 as of July 29) predate this sprint — not introduced by Follow-Through, left alone to avoid unrelated scope creep, but worth a quick cleanup pass.
- 🟢 New routes (/api/follow-through/today, /api/follow-through/report) both carry force-dynamic — verified.
- 🟢 No new cron entries added for Follow-Through — expiry is lazy (checked on read in /today), deliberately avoiding a 4th route on an app already flagged for nearing the Vercel Hobby cron cap.

### Audit findings — July 30 code review (Spark extension + Ritual enrichment)
- 🟢 All 6 new routes this sprint (spark/reveal, ritual/revisit-check, ritual/revisit-respond, ritual/partner-note, plus modified follow-through/today, bet/respond, spark/respond, ritual/checkin, ritual/retire, dashboard/hero) carry force-dynamic — verified individually.
- 🟢 No debug console.log left in any touched file — checked every file this sprint touched, only console.error remains, matching the existing convention.
- 🟢 No new cron entries added — the Ritual revisit mechanic's dormancy check runs inline when the Library view loads (same lazy-check pattern as Follow-Through's expiry), not on a schedule.
- 🟢 Two real pre-existing bugs found and fixed while building on top of this code, not invented scope: the /today source-reveal gate (see Daily Rhythm above) and the discovering-phase retire persistence gap (see Daily Rhythm above). Both were caught by tracing the exact mechanism a new feature needed to depend on, root-caused, and fixed before building on top of them, per protocol.
- 🟢 lib/follow-through.js and lib/ritual-retire.js extractions verified as pure refactors — re-read both call sites (bet/respond, spark/respond for the first; ritual/retire, ritual/revisit-respond for the second) to confirm no behavior change, only de-duplication.
- 📋 Same 10 pre-existing react/no-unescaped-entities lint errors in components/RitualCard.js and the same pre-existing 5 in app/dashboard/page.js persist (line numbers shifted from July 29's count as content was added around them) — none introduced this sprint, still not worth a dedicated cleanup pass on their own.
- 📋 Not yet live-tested end to end: Spark's Follow-Through generation (next Monday/Thursday, or a forced `?spark=true`, is the first real chance) and all four Ritual enrichment pieces (next Friday, or a forced ritual check-in, is the first real chance).

### Audit findings — July 30 code review (Wednesday/Thursday rollout)
- 🟢 No new routes this piece — only app/api/cron/scheduled-tasks/route.js (already force-dynamic), lib/follow-through.js, app/api/follow-through/today/route.js, and app/dashboard/page.js were touched.
- 🟢 No debug console.log added — checked all four touched files.
- 🟢 No new cron entries — Follow-Through generation was added inside the existing processWednesdayReveal/processWednesdayCutoff/processThursdayReveal cron functions, not a new schedule.
- 🟢 generateFollowThrough's new myQuestion/theirQuestion params verified backward compatible — when omitted, both fall back to sourceQuestion and take the identical prompt branch as before existing Bet/Spark/Wednesday callers pass sourceQuestion only.
- 📋 Not yet live-tested end to end: both fire on next Wednesday/Thursday's actual cron run, or a forced `?notice=true`/`?thursday=true` dashboard visit combined with manually invoking the cron route with the right UTC hour — genuinely harder to force-test locally than Bet/Spark/Ritual since the reveal functions gate on exact UTC day/hour matches, not URL overrides.

### Audit findings — July 30 code review (Security fix rollout, Tier 1)
- 🟢 Pattern verified sound before scaling: requireUser() reuses the exact auth.getUser(token) mechanism already proven in 44 pre-existing routes (spark/today, notebook/entry/[id], etc.), just extracted once. verifyCoupleMembership() closes a real gap found in some of those same "good" routes (timeline/event validated a session existed but still trusted a client-supplied coupleId for the write — blocks anonymous callers only, not cross-account impersonation between two authenticated users).
- 🟢 Deploy-atomicity confirmed as the reason paired route+caller commits are safe here: client and API routes ship in the same Next.js build/deploy, so there's never a window where a route requires a header a live client isn't sending yet.
- 🟢 lint-clean on every touched file in Tier 1 (bet/react, bet/reveal, bet/respond, ritual/checkin, ritual/adopt, ritual/confirm, ritual/partner-note, ritual/retire, ritual/revisit-check, ritual/revisit-respond, ritual/start, ritual/status, follow-through/report, follow-through/today, dashboard/hero, dashboard/memory, plus BetCard.js, RitualCard.js, FollowThroughCard.js, app/dashboard/page.js, app/ritual/page.js, app/us/page.js) — only the same pre-existing react/no-unescaped-entities errors already logged in prior sprints, not introduced this pass.
- 🟢 force-dynamic already present on every touched route (all pre-existed this sprint) — verified, no regressions.
- 🟢 bet/respond's resolvedCoupleId priority flipped from `coupleId || betRow.couple_id` to `betRow.couple_id || coupleId` — DB-derived value now takes priority over client-supplied, consistent with the "never trust the client for identity/ownership" principle this whole fix is built on.
- 📋 Found but out of scope, not fixed: app/ritual/page.js's handleSaveEdit calls a `/api/ritual/update` route that does not exist anywhere in app/api/ritual/ (pre-existing 404, predates this sprint — the "See all rituals" library page's ritual-edit save silently fails). Worth a dedicated fix, deliberately not bundled into the security sprint.
- 📋 Not yet live-tested end to end (no session token / live Supabase session available in the sandbox) — verified via lint + full read-through of both the server route and its client caller for each conversion; real confirmation happens when Matt exercises Bet, Ritual, Follow-Through, and the dashboard hero/memory cards live.

### Audit findings — July 31 (Memory Test unlock gate + question generator)
- 🟢 Root cause traced via git log -S rather than guessed: commit 267b830 "temp: unlock Memory mode for testing (thresholds set to 0)" zeroed lib/challenge-prompts.js's MEMORY_UNLOCK from its real original values (minTimelineEvents: 5, minSparkBetResponses: 10, minAccountAgeWeeks: 3) and was never reverted — confirmed via Matt's own recollection that the gate was intentional design (Memory Test needs real shared history to draw on, so brand-new couples can't play it meaningfully).
- 🟢 Second independent bug found in the same dead code: the original eligibility check queried account age via `user_profiles.eq('id', userId)` — every other route in the codebase uses the `user_id` column, not `id`. That query would always return null, so even with real thresholds the account-age check could never pass. Fixed by checking `couples.created_at` instead, which is also a better fit — a shared unlock should key off how long the couple has been in the app together, not one partner's individual signup date.
- 🟢 lib/memory-unlock.js built as a single shared helper (checkMemoryUnlocked) so server-side enforcement (confirm-type) and the client-side tile UI (game-room home) can never drift out of sync with each other.
- 🟢 Enforcement added server-side in confirm-type, not just hidden in the UI — same "never trust the client" principle as the whole BOLA sprint. Directly hitting confirm-type with challengeType=memory now 403s if the couple isn't eligible, regardless of what the home page tile shows.
- 🟢 Found and fixed a real UX gap while wiring the tile: game-room/page.js hardcoded the Memory Test tile's `available: false` for literally every couple regardless of progress — the dynamic per-couple unlock had never actually been built into the current UI at all, even before the thresholds were zeroed. MEMORY_LOCKED_COPY was already written and imported but never rendered anywhere; now shown on tap when locked, with a CTA into /us/add.
- 🟢 Bigger find: game-room/challenge/generate's 'memory' branch (the route that actually writes Memory Test's question content) was reading Spark/Bet history from `today_responses` and `bets.user1_answer`/`user2_answer` — neither is written by any live route anymore (the app moved to the `spark_responses`/`bet_responses` per-user-row model at some point and this one code path never got updated). Practically this meant the "Recent Spark answers" / "Recent Bet answers" sections of Nora's prompt came back empty for every couple, no matter how much real history existed — only Timeline events and Nora's memory summary were ever real. Rewrote both queries to embed the live tables (`spark_responses` → `sparks(question)`, `bet_responses` → `bets(question)`), same embedding pattern already proven working elsewhere (lib/nora-memory.js, cron/scheduled-tasks). This is the fix that actually lets Memory Test draw on a couple's real accumulated history, which was Matt's original ask.
- 🟢 lint-clean on all 6 touched/new files (lib/challenge-prompts.js, lib/memory-unlock.js, confirm-type, generate, game-room/page.js, lobby/page.js) — only the same pre-existing react/no-unescaped-entities error in lobby/page.js already logged in prior sprints.
- 📋 Not yet live-tested end to end — real confirmation happens when Matt & Cass's couple (or a forced test couple past the thresholds) actually plays a Memory Test round and the generated questions visibly reference real Spark/Bet/Timeline content.
- 🟢 Signals audit complete. Checked lib/nora-memory.js's SIGNAL_TYPES (16 total) against actual call sites — all 16 are live, not defined-and-forgotten: Spark, Bet, Weekly Reflection, Ritual check-ins, Flirts sent/received, Dates completed, Timeline events, Ahead (shared items), Notebook, Practices, Assessment, Follow-Through, AI coach conversations, and every Game Room sub-game's debrief. So nearly everything already reaches Memory Test indirectly via `noraMemory.memory_summary`, which is included in every generation prompt. The real gap was direct vs. indirect: only Timeline (and now Spark/Bet after the earlier fix) were pulled as raw, specific, quotable data — everything else only reached Memory Test through one compressed prose summary, which loses the concrete detail a real "remember when" question needs.
- 🟢 Wired in Dates and Flirts as direct sources, commit 2603e52, same pattern as Spark/Bet/Timeline: completed dates contribute real reactions/reviews (custom_dates, using the same REACTION_LABELS as the Date Night UI), sent Flirts contribute their content (normalising the two different schemas — Nora-generated flirts use mode+suggestion, freeform ones sent via FlirtCard use type+content).
- ✅ Real privacy finding, fixed July 31, commits 6eecfdb (schema) + ae19dcd (code). AI coach conversations (SIGNAL_TYPES.NORA_CONVERSATION) were feeding into the couple-shared `memory_summary` that Memory Test (and other couple-facing Nora surfaces) reads. Traced the full path in lib/nora-memory.js: NORA_CONVERSATION was correctly excluded from `SHARED_SIGNALS` (never wrote directly to `couple_notes`), but it WAS in `INDIVIDUAL_NOTE_SIGNALS`, so a private AI coach conversation got synthesized into that person's `user1_notes`/`user2_notes` — and `buildMemorySummaryPrompt`/`extractStructuredFacts` then blended BOTH partners' individual notes into the shared `memory_summary`/`couple_notes.structured_facts`, with no instruction to keep one-sided disclosures confidential. Matt flagged this directly: "The individual is sharing individually with Nora and that content should not be used." Designed as Sr-Dev-Lead per Matt's explicit request, reviewed against two of his specific concerns (would this break existing Nora reasoning? would any of Cass/Matt's existing individual or couples work be lost?) before building — Matt's instruction was explicit: "Do not Build until we have discussed and agreed."
  - Root cause fix, not a downstream filter: new `nora_private_notes` table (docs/database/nora-private-notes.sql, one row per user_id, RLS-scoped to owner) holds Nora's synthesis of that person's AI-coach conversations only. `updateNoraMemory` now intercepts NORA_CONVERSATION with an early return that writes here instead of `user1_notes`/`user2_notes`, reusing the existing `buildPersonNotesPrompt` unchanged — only the destination changed. Removed NORA_CONVERSATION from `INDIVIDUAL_NOTE_SIGNALS` since it's intercepted before that code runs. `buildMemorySummaryPrompt`/`extractStructuredFacts` (the two functions that produce every couple-facing derived output — Memory Test, dashboard hero's structuredFacts, Follow-Through, Wed/Thu cron reveals, every Game Room debrief) now simply never receive AI-coach content as input, so every downstream consumer is fixed automatically with zero changes needed at each individual surface.
  - Continuity preserved, not discarded: confirmed 3 call sites read `user1_notes`/`user2_notes` outside nora-memory.js. app/api/ai-coach/route.js's opener computes `myNotes`/`memorySummary` but never actually uses them in its response (dead code, confirmed via grep — each var appears once, at its own declaration) — no change needed. app/api/dashboard/hero/route.js's `myPersonNotes` and app/api/me/synthesis/route.js's `userNotes` are both genuinely active and strictly self-facing (hero card / weekly notebook synthesis, addressed to and only ever shown to that one user, never their partner) — both now merge in the new `getPrivateNotes(userId)` alongside the existing couple-context notes, so Nora still knows what that individual told her privately when speaking to them alone, exactly as Matt asked: "we need Nora to know what the individual has said and how it applies to the couple" for that same person's own views.
  - Nothing existing was reset or nulled. This only changes where NEW AI-coach-sourced content gets written going forward; Matt and Cass's existing `user1_notes`/`user2_notes`/`couple_notes`/`memory_summary` are untouched by this commit. A separate one-shot rebuild (backlog item below) is still needed to retroactively clean any AI-coach content already blended into their existing notes from before this fix — that rebuild will run in dry-run/preview mode and be shown to Matt before writing anything live, per his explicit instruction.
  - ✅ Migration run in Supabase SQL Editor by Matt, July 31 — nora_private_notes table is live. Fix is fully deployed end to end.
  - ✅ One-shot rebuild script built, commit c37f667: `scripts/rebuild-nora-memory.mjs`. Reconstructs `user1_notes`/`user2_notes`/`couple_notes`/`memory_summary`/`structured_facts` from the couple's real `nora_signals` history with `nora_conversation` excluded — batched incremental synthesis (chronological groups of 20 signals, not a literal per-event replay) using the real production prompt builders and routing tables imported directly from `lib/nora-memory.js` (required fixing two extensionless relative imports — `lib/nora-memory.js`'s `./nora`/`./nora-knowledge` and `lib/nora.js`'s `./nora-knowledge` — to explicit `.js` extensions so the script can import real code instead of duplicating it; confirmed harmless under Next.js). Three explicit, separately-gated modes: `--counts` (free, no writes), `--preview` (full synthesis, writes to `scripts/output/*.json`+`*.md`, zero DB writes), `--apply=<path>` (backs up the live row first, then applies exactly the reviewed preview file — not a re-generation). Must be run locally by Matt (this sandbox has no network route to Supabase or Anthropic); verified as far as the sandbox allows — lint-clean, `node --check` passes, import chain resolves, reaches the expected network call.
  - 📋 Real gap found while building this, not fixed (out of scope for this pass): `ASSESSMENT_COMPLETE` signals are logged to `nora_signals` and have a full prompt lens written in `buildPersonNotesPrompt` ("the foundation everything else builds on"), but the signal type is missing from `INDIVIDUAL_NOTE_SIGNALS` — so completing the relationship assessment does not currently update `user1_notes`/`user2_notes` in production at all, despite `app/api/assessment/seed-memory/route.js` calling `updateNoraMemory` with that exact signal type. The rebuild script matches current production behavior (excludes it) rather than silently fixing this in the same pass. Needs its own decision: add `ASSESSMENT_COMPLETE` to `INDIVIDUAL_NOTE_SIGNALS`, or confirm assessment data is meant to reach notes some other way.
  - ✅ CLOSED, decision made Aug 3 — historical data left untouched, script built but deliberately not applied. Ran `--list-couples` (found a second, orphaned test couple with no real user2 and zero signals — left alone, unrelated), `--counts` (real couple: 40 non-conversation signals), then `--preview` (~8 LLM calls, produced a specific, well-formed reconstruction with no conversation-derived content — full text in scripts/output/nora-memory-rebuild-preview-2026-08-03T18-02-54-082Z.md). Matt reviewed and asked the right question: does `--apply` keep everything the same, minus the bleed? Honest answer: no — `--apply` is a full replacement built from only 40 activity signals, not a surgical removal of conversation-derived content from the existing notes (the current notes have no per-sentence source attribution, so a true surgical scrub isn't cleanly possible; only a full rebuild or an LLM-judgment edit pass, and the edit pass carries the same degradation risk). Matt's bottom line: the forward-only fix is what actually matters ("it works clean for new users"); for Matt & Cass's own already-accumulated relationship, preserving the effort already built matters more than retroactively purging historical conversation-influence from it. Decision: do not run `--apply`. `user1_notes`/`user2_notes`/`couple_notes`/`memory_summary` for couple 8230e60f-44ca-4668-be28-06cb32b1b831 remain exactly as they were before this entire investigation — nothing regenerated, nothing deleted. The July 31 fix (commit ae19dcd) still fully applies going forward: new AI-coach conversations route to `nora_private_notes` only, never the couple-facing layer. `scripts/rebuild-nora-memory.mjs` stays in the repo, unused for now, available if this decision is ever revisited (e.g., for a future couple that wants a clean rebuild, or if Matt changes his mind).

### Audit findings — Aug 3 (Daily Rhythm feature audit + fix sprint)
- ✅ Full audit of the day-to-day/week-to-week engagement loop (Dashboard hero, Spark, Bet, Ritual, Weekly Reflection, Wednesday/Thursday cron reveals, Follow-Through, plus the retired Daily Check-in leftovers) — dashboard hero and Bet came back clean; every other area had at least one real finding, listed below.
- ✅ Real security findings — 4 feature areas the "complete" July 30 71-route BOLA sprint actually missed, all fixed same day. Spark (commit 614e4e0): `spark/skip` trusted a client-supplied coupleId with zero ownership check and would overwrite ANY couple's live sparks row given a guessed sparkId — now derives coupleId from the sparks row itself and verifies membership; `spark/respond` derived coupleId correctly but never verified membership before upserting a response, writing into `nora_memory.memory_summary`, and firing Follow-Through generation; `spark/react` confirmed already safe by construction (self-scoped to the caller's own row), refactored to the shared helper for consistency only. Weekly Reflection (commit e933d57) — the whole feature wasn't in the original sprint's scope at all: `reflection/generate` returned another couple's real reflection content (Spark/Bet answers, private observations) to anyone supplying their coupleId, same class of leak as the original `trips/wander` bug; `status`/`viewed`/`react` had the same missing-membership gap, and `viewed` additionally trusted a client-supplied userId to decide which partner's flag to flip. Wednesday/Thursday (commit a6d9969) — all four routes fell through to treating any non-member caller as "user2," so `wednesday/send`/`thursday/respond` would let a stranger overwrite the real user2's notice/response text with their own, not just a read leak. Follow-Through (commit 2477cf1) — `report` verified the caller belonged to their own supplied coupleId but never checked the target followThroughId row's own couple_id matched, a confused-deputy gap on the resource-ID axis (same shape lib/api-auth.js's docstring calls out) rather than the coupleId axis; fixed the same way ritual/partner-note already did, with a couple_id-scoped fetch.
- 🟢 Ritual UI gap fixed, commit c002ed0: the July 30 enrichment sprint built a live, per-checkin Nora reaction (`ritual_completions.nora_reaction`) but never displayed it — app/ritual/page.js's STILL TRYING card always showed the static `NORA_WEEK_MESSAGES[weekNum]` copy even when a real reaction existed for that ritual this week. `ritual/status` already returned this week's completions; the client just never read the field. Now shows the live reaction first, falling back to the static copy only for a ritual with no check-in yet this week (a real empty state, not the bug).
- ✅ Known `handleSaveEdit` 404 fixed, commit 283c529 (originally flagged and deliberately left out of scope in the July 30 sprint — see that section above). Built `app/api/ritual/update` (requireUser + verifyCoupleMembership + couple_id-scoped UPDATE on title/description, same confused-deputy guard as follow-through/report). Also found and fixed a second bug on the same handler: it was missing the Authorization header every sibling handler in the file sends, so it would have 401'd even pointed at a real endpoint.
- 📋 Confirmed dead code, not touched: `lib/spark-questions.json` (26KB, never read — `SPARK_QUESTIONS` in `lib/spark-questions.js` is a hand-written array), `lib/ritual-suggestions.js`'s `getRitualsByTag`/`getRitualsByTier`/`getSeasonalRituals` (only `getStarterRituals` is live), `lib/checkin-questions.js` (reconfirmed zero imports, unchanged since July 28), `components/ReflectionCard.js` (a second, unrelated Weekly Reflection card component with zero import references anywhere and no Authorization headers on any of its API calls — never wired into any page). Also reconfirmed: `app/api/spark/skip`'s Skip button in SparkCard.js never actually calls the route at all (`onSkip` just refetches) — same class of dead code as bet/lock etc., the security fix was applied anyway since it's harmless and correct either way, but the route currently has no live caller.
- 📋 Real, separate gap found and logged (not fixed): `lib/checkin-patterns.js`'s `analyzeUserPatterns` (still called live from `app/ai-coach/page.js`) keys its entire concern-detection logic off `mood`/`connection_score` columns that none of the three live writers into `daily_checkins` (spark/respond, bet/respond, ritual/checkin) ever populate — so `moodToNumber(undefined)` always defaults to 3, and 3 of the function's 4 concern checks can never fire against real data. Worth checking whether the live Supabase `daily_checkins` table still enforces the `NOT NULL` constraints `docs/database/daily-checkins-v2.sql` declares for those columns — if so, every write from those three routes may be silently failing (none of them check the upsert's error return). Also found: `docs/database/daily-checkins.sql` (older, structurally incompatible schema) and `daily-checkins-v2.sql` are both present and describe two different shapes for the same table — stale/conflicting docs, worth cleaning up.
- 📋 Real, separate gap found and logged (not fixed): Wednesday/Thursday content only reaches Nora's couple-facing memory indirectly, through Follow-Through's signals fired inside `processWednesdayReveal`/`processThursdayReveal` — there's no direct `SIGNAL_TYPES` entry for either feature. Since Follow-Through generation is itself gated (distress checks, ~10% wildcard eligibility), a typical week where it doesn't fire means that week's actual Notice/Observation content never reaches Nora's memory at all, unlike Spark/Bet which write their own signal every time both partners answer.
- 🟢 lint-clean on every touched/new file (spark/skip, spark/respond, spark/react, reflection/status, reflection/viewed, reflection/react, reflection/generate, wednesday/today, wednesday/send, thursday/today, thursday/respond, follow-through/report, app/ritual/page.js, ritual/update) — only 3 pre-existing react/no-unescaped-entities errors in app/ritual/page.js, confirmed via diff to be untouched by this sprint's changes.
- 🟢 Every client caller checked against its route's new contract before committing (app/dashboard/page.js, app/weekly-reflection/page.js, components/WednesdayCard.js, components/ThursdayCard.js, components/FollowThroughCard.js, app/ritual/page.js) — all already sent proper Authorization headers and matching payload shapes; no client changes needed beyond handleSaveEdit's missing header.
- 📋 Not yet live-tested end to end — real confirmation happens when Matt & Cass exercise Spark, Weekly Reflection, Wednesday/Thursday, Follow-Through, and Ritual editing live.

### Audit findings — Aug 3 (Nora memory/signals security sprint)
- ✅ Full audit of the Nora memory/signals layer end to end — every route that reads or writes `nora_memory`, `nora_signals`, `nora_private_notes`, or `nora_claims`. This turned up the most severe findings of the entire engagement: multiple full data-exfiltration/injection paths into arbitrary couples' private therapy-grade notes, on top of the already-completed July 30 71-route BOLA sprint and the Aug 3 Daily Rhythm sprint. Fixed in priority order Matt approved: critical first, then a contained Tier 2, then this wiring-gaps writeup.
- ✅ CRITICAL — commit 1fda581: `ai-coach/route.js` (both GET and POST) had zero couple-membership check on a client-supplied coupleId, and POST additionally accepted any `conversationId` with no ownership check at all — any authenticated user could read or append to any other couple's AI-coach conversation, and `classifyClaimResponse` (the only real caller found for it) meant a crafted request could also inject fabricated claim-corrections into a stranger's `nora_claims` history. Switched to `requireUser`/`verifyCoupleMembership`, added a `conversationId` ownership check (`ai_conversations.user_id === user.id`) on both verbs.
- ✅ CRITICAL — commit 17b91eb: `ai-coach/close-session/route.js` had the identical conversationId-ownership gap as the route above, letting a crafted request close out and synthesize memory from another user's private coaching session. Same fix pattern.
- ✅ CRITICAL — commit a09b927: `timeline/event/route.js` accepted both `userId` and `coupleId` straight from the client with no verification of either — full BOLA on both axes. An earlier July 30 backlog entry had claimed this exact route was already covered by that sprint; it wasn't (the live file had no `requireUser`/`verifyCoupleMembership` import at all), so that entry was stale — either the sweep missed this route or fixed a differently-named one. Switched to `requireUser`/`verifyCoupleMembership`; `userId` is no longer accepted from the client at all, now always `user.id`.
- ✅ CRITICAL — commit 590aa50: `nora-inline/route.js` GET and POST both had zero couple-membership check on a client-supplied coupleId — direct read/write access to any couple's inline Nora context. Added `verifyCoupleMembership` to both, switched to `requireUser`.
- ✅ Tier 2 — commit 527889d (5 files, all lower-severity/contained, all client callers individually verified against the new contracts before committing): `flirts/react` fetched the flirt row to fire a `FLIRT_RECEIVED` signal without scoping to `receiver_id = user.id` — the preceding UPDATE already correctly no-opped for a flirt that wasn't theirs, but the unscoped SELECT let any caller supply another couple's flirtId and inject a fabricated reaction into that couple's shared `couple_notes`. `game-room/generate-debrief` and `challenge/rank/finalize` were both missing `userId` on their `updateNoraMemory` calls — since `GAME_ROOM_DEBRIEF` is individual-only in `INDIVIDUAL_NOTE_SIGNALS`, a missing userId silently dropped the individual-notes write entirely for every Rabbit Hole debrief and Rank Challenge finalize, even though the couple-level signal count still credited both partners. `game-room/call/verdict` accepted a client-supplied `predictorUserId` with no validation against the real couple's `user1_id`/`user2_id` — now validated server-side against the couple fetched from the session's own `couple_id`. `practices/[id]` had its `updateNoraMemory` coupleId priority inverted (`coupleId || existing.couple_id`, client-first) — flipped to match the `bet/respond` precedent (DB-derived value takes priority); the row's own auth was already correctly scoped to `user_id = user.id`, so this only affected which couple a signal got logged against, not actual data exposure.
- ✅ Defense in depth — commit 2ed053d: `practices` POST and `notebook/entry` POST both accepted an optional client-supplied `coupleId` used both for the row insert and passed into `updateNoraMemory`, with no verification. Fixed by deriving `coupleId` from the caller's own profile via the existing `getOwnCoupleId` helper instead of trusting the client at all — same guidance `lib/api-auth.js`'s own docstring gives for routes that don't need to accept an arbitrary coupleId in the first place. Actual notes content was never at risk here even before the fix (`updateNoraMemory` only writes into a `user1_notes`/`user2_notes` slot if the acting userId matches that specific couple's real members), but a crafted coupleId could log junk into another couple's `nora_signals` and force an unnecessary `memory_summary` regeneration against their `nora_memory` row.
- 📋 Real functional gap found while fixing the item above, not a security issue: neither route's actual client caller (`app/profile/page.js`) has ever sent a `coupleId` — POST bodies are `{ title }` and `{ entryType, content }` only. So `coupleId` was always `null` in production, `updateNoraMemory`'s `.eq('id', null)` couple lookup always returned nothing, and the function returned early — meaning `PRACTICE_ADDED`, `PRACTICE_UPDATED`, and `NOTEBOOK_ENTRY` signals have never actually reached Nora's memory at all for any real user. The coupleId fix above is what wires these up for the first time as a side effect.
- 📋 Signal-routing gaps found during the audit, logged for a future product decision (no fix applied — same "found but not fixed, needs a call" treatment as the `ASSESSMENT_COMPLETE` gap logged above): `FLIRT_RECEIVED` has a full prompt lens in `buildPersonNotesPrompt` ("what the receiver's reaction reveals about how they receive love and desire") and earns both individual and couple signal-count weight, but is missing from both `SHARED_NOTE_SIGNALS` and `INDIVIDUAL_NOTE_SIGNALS` — so a flirt reaction updates `couple_notes` and the signal counts, but the specific per-person hypothesis the prompt was written for never actually reaches that person's `user1_notes`/`user2_notes`. `PRACTICE_ADDED`, `PRACTICE_UPDATED`, and `SHARED_ITEM_COMPLETED` are all missing from `INDIVIDUAL_SIGNAL_WEIGHTS` (practices are correctly routed to individual notes via `INDIVIDUAL_NOTE_SIGNALS`, and shared items correctly update both partners' notes via `SHARED_NOTE_SIGNALS`, but neither ever increments `user1_individual_signal_count`/`user2_individual_signal_count`) — meaning any signal-count-gated feature (e.g. the Memory Test unlock thresholds built earlier this engagement) systematically undercounts real engagement from practices and shared-item completions. `MEMORY_REFLECTION` updates individual notes and its own signal count correctly, but is absent from `COUPLE_SIGNAL_WEIGHTS`/`SHARED_SIGNALS` entirely, so revisiting a shared timeline memory never reaches `couple_notes` — plausibly intentional (keeping it individual-only), but worth a explicit product call rather than leaving it as an unexamined asymmetry. Separately, `flirts/save-profile` reuses the `FLIRT_SENT` signal type for a structurally different action (building a flirt profile from a conversation, not sending a flirt) — technically harmless but means Nora's `FLIRT_SENT` notes lens ("what the choice of flirt reveals...") gets applied to profile-building content it wasn't written for; may be worth its own signal type.
- 🟢 lint-clean on all 11 touched/new files across the full sprint (`ai-coach`, `ai-coach/close-session`, `timeline/event`, `nora-inline`, `flirts/react`, `game-room/generate-debrief`, `game-room/challenge/rank/finalize`, `game-room/call/verdict`, `practices/[id]`, `practices`, `notebook/entry`).
- 🟢 Every client caller checked against each route's new/changed contract before committing — no request/response shape changes anywhere in this sprint, only auth tightening, an internal signal-logging fix, and one new authorization check (`call/verdict`'s predictorUserId validation) that the real client already satisfies by construction.
- 📋 Not yet live-tested end to end — real confirmation happens when Matt & Cass exercise AI coach, flirts, Game Room (Rabbit Hole debrief, Rank Challenge, The Call), practices, and notebook entries live.

### Audit findings — Aug 4 (Game Room/Memory/auth security sprint)
- ✅ Full audit of the last unaudited surfaces this engagement — Game Room (31 route files across The Call, The Challenge, The Hunt, Hot Take, and Rabbit Hole, plus the shared lobby/session layer), the couple-pairing/connect-code flow, and onboarding. Two parallel subagent sweeps, then personal verification of the most severe claims before reporting anything to Matt as confirmed, per this engagement's standard practice. Found the single most severe vulnerability of the whole engagement.
- ✅ CRITICAL, code deployed commit 4dc5a55, RLS migration written but not yet applied (`docs/database/couples-rls-fix.sql`) — full account/relationship takeover via the `couples` table. Live policies, pulled via `pg_policies` rather than assumed: the UPDATE policy's "join as partner" branch only checked that a row was unpaired and had *some* `connect_code`, never that the caller supplied the matching one; the SELECT policy exposed every couple row to any authenticated user via the same unbounded `connect_code IS NOT NULL` branch. Combined, any authenticated user could enumerate every unpaired couple and pair themselves in directly with zero code-guessing — full takeover, with downstream access to that couple's entire memory/timeline/assessment data via `verifyCoupleMembership` everywhere else in the app. Root-cause fix: two new service-role routes (`api/couples/create-code`, `api/couples/join`) that re-verify everything server-side instead of trusting RLS wording, replacing every raw client-side Supabase call in `app/connect/page.js` and a second, duplicate client-side implementation found in `app/onboarding/page.js`'s `prepareStep4`. The RLS migration is defense-in-depth on top of that — locks both policies down to self-only — and must be run by Matt in the Supabase SQL Editor only *after* this code is deployed, since it removes the policy branches the old client code depended on.
  - Found and fixed in passing: `user_profiles.couple_id` was never being set for a joining partner (user2) anywhere in the app — only the couple-creator's onboarding flow set it. This could have silently broken any `getOwnCoupleId()`-dependent route (practices, notebook/entry, fixed in the Aug 3 Nora memory sprint above) for anyone who joined via connect code rather than created the couple. Both new routes now upsert it for whichever party they touch.
- ✅ CRITICAL, commit d35c645 — `game-room/challenge/start` had zero authentication at all: userId/coupleId taken straight from an unauthenticated request body, used to read `nora_memory.memory_summary` and `user_profiles.love_language`/`attachment_style` and feed both into an AI prompt. No live caller currently exists in `app/game-room` (unused by the current UI) but was reachable directly regardless — fixed the same as every other unused-but-vulnerable route found this engagement. Second bug found in the same file: it queried account age via `user_profiles.eq('id', userId)` instead of `.eq('user_id', userId)` — same wrong-column mistake already found and fixed in the July 31 Memory Test sprint, in a different file — meaning `accountAgeWeeks` was always 0 and that leg of the route's own eligibility check could never pass.
- ✅ Confused-deputy write bugs — 9 routes total, same underlying shape everywhere: `verifyCoupleMembership` only proves the caller belongs to whatever `coupleId` they send, never that a separately client-supplied resource id (sessionId, roundId, etc.) actually belongs to that couple. Fixed by deriving the real couple_id from the resource's own row wherever the table has one (strictly safer than checking-then-trusting a client value), or by scoping the write query with an extra `.eq()` where deriving isn't practical:
  - `round-ready` (commit b357d31) — `game_rounds` had no couple_id column of its own; added a `game_sessions.couple_id` check before touching it.
  - `generate-hole` (commit fb9bea2) — session read/write and `game_rounds` insert all reachable cross-couple via a guessed sessionId.
  - `hunt/drop` (commit 124934a) — rewritten to derive couple_id from `hunt_sessions` itself, matching the already-clean pattern in `hunt/confirm`.
  - `hunt/debrief` (commit 084f8c6) — same pattern; also closed a private-data leak, since a guessed sessionId would have fed another couple's Spark/Bet/Timeline data and Nora memory into the debrief-verdict AI prompt.
  - `hot-take/answer` (commit 79aea4b) — derives couple_id from `hot_take_sessions`; previously a guessed sessionId let the upsert overwrite another couple's answer row's couple_id field entirely.
  - `call/generate` + `call/answer` (commit 19f6a72) — `call/generate` now derives couple_id from `call_sessions` (matching `call/answer`'s already-clean pattern) and validates `hotSeatUserId` is actually one of the couple's two members, closing a third-party profile-leak path into the Nora prompt. `call/answer` had its own separate gap on the resource-ID axis — `roundId` was never checked against `callSessionId` — fixed by scoping the update query; also stopped trusting a client-supplied `isHotSeat` flag, deriving it server-side from the round's real `hot_seat_user_id` instead, since a predictor claiming `isHotSeat:true` could otherwise overwrite the hot seat's own answer field.
  - `challenge/generate` + `challenge/memory/ready` (commit d912d06) — both now derive couple_id from `challenge_sessions`, matching the already-clean pattern in `challenge/next`. `challenge/generate`'s gap would have leaked a couple's private Spark/Bet/Timeline/date/Flirt history into the memory-question AI prompt; `challenge/memory/ready`'s let a caller send their own real (membership-passing) coupleId alongside a different couple's sessionId and mark that couple's round ready / overwrite its answer.
- ✅ Cross-couple read-leak routes — 4 routes, same shape as above but read-only: a client-supplied `sessionId` never checked against the verified `coupleId`, letting a caller read back (and, for the two `*/start` idempotency checks, corrupt) another couple's session state. Fixed with a `game_sessions.couple_id` check, commit cdd1d81: `nora-nudge` (would have echoed another couple's private `hole_topic`/`hole_entry` back inside the generated nudge text), `hot-take/start`, `call/start`, `hunt/start` (all three would have returned another couple's already-picked questions/mission, or inserted a second session row for that session_id tagged with the wrong couple_id if none existed yet).
- 🟢 lint-clean on every touched/new file this sprint (`api/couples/create-code`, `api/couples/join`, `app/connect/page.js`, `app/onboarding/page.js`, `game-room/challenge/start`, `game-room/round-ready`, `game-room/generate-hole`, `game-room/hunt/drop`, `game-room/hunt/debrief`, `game-room/hot-take/answer`, `game-room/call/generate`, `game-room/call/answer`, `game-room/challenge/generate`, `game-room/challenge/memory/ready`, `game-room/nora-nudge`, `game-room/hot-take/start`, `game-room/call/start`, `game-room/hunt/start`) — the only errors found (`react/no-unescaped-entities` at 2 lines in `connect/page.js`, 3 in `onboarding/page.js`) are pre-existing JSX apostrophes, confirmed via `git show` to be untouched by this sprint's diff.
- 🟢 Every client caller checked against its route's new/changed contract before committing — no request/response shape changes anywhere this sprint; several routes now simply ignore an inert extra field the client still sends (e.g. `hunt/drop`'s `coupleId`, `call/answer`'s `isHotSeat`), which is harmless by design and needs no client-side change.
- 📋 Found, flagged, deliberately not restored or deleted: `app/api/game-room/lobby-status/.junk_1785522192352058088` — a leftover file from the July 31 dead-code cleanup (the live `lobby-status` route was deleted as confirmed-dead, commit 687d560) containing a fully unauthenticated GET handler with a service-role client and the exact same class of bug as this sprint's read-leak routes (any caller can read any couple's `game_sessions` row by guessing a coupleId, no auth at all). Currently inert — the filename doesn't match Next.js's `route.js` convention, so it isn't a reachable route — but it's a landmine if anyone ever renames/restores it without noticing. Left as-is rather than deleted, since it's already effectively dead; worth a deliberate decision (delete outright, or restore properly fixed) rather than leaving it to be rediscovered by accident.
- 📋 Not yet live-tested end to end — real confirmation happens when Matt & Cass connect via a real connect code, and exercise Rabbit Hole, The Hunt, Hot Take, The Call, and The Challenge (including a Memory Test round) live. The couples-table RLS migration still needs Matt to run it in the Supabase SQL Editor — after this sprint's code is confirmed deployed, not before.

### Bug fixes — Aug 4 (post-sprint live bug reports)
- ✅ Memory Test fixed, commit 134cab0 — Matt's exact repro ("both reach the lobby, launch, get 'Something went wrong loading the challenge', Try Again does nothing") traced to `challenge/generate`'s round-save section: it upserted with `ignoreDuplicates:true` (the intended fix for both partners' clients calling generate for the same round simultaneously) but then called `.select().single()` — on the losing side of that race, the upsert matches zero rows, and `.single()` on a zero-row result returns a PostgREST error object the code never checked, instead of the `null` `.maybeSingle()` would give. Switched to `.maybeSingle()` throughout (the upsert-select, the fallback re-fetch, and the earlier idempotency check), and added `console.error` logging to the upsert error path and the outer catch-all, which previously had zero logging — nothing reached Vercel's logs when this failed.
- ✅ Bet questions category field built out, commit 96f4010 — tagged all 120 questions in `lib/bet-questions.js` (11 preferences, 58 likely, 16 reactions, 35 confessions) and added category-variety weighting to `getBetQuestion()`, mirroring its existing level-weighting pattern: down-weights whichever category showed up most in the couple's last 3 bets, floored so no category is ever fully excluded. `question_category` had been written to every `bets` row since its addition but never read by anything — this is the first real consumer.
- ✅ `lib/checkin-patterns.js` fully rebuilt, commit da498bd — confirmed via a live column query that `daily_checkins.mood`/`connection_score` are nullable and have been null on every row since the scored/emoji check-in UI was killed (no live writer — `bet/respond`, `spark/respond`, `ritual/checkin` — has ever populated them). The old `analyzeUserPatterns` silently ran on defaults and had an active false-positive: JS coerces `null` to `0` in a numeric comparison, so `connection_score < 3` was true for every row, meaning the old `low_connection` concern fired unconditionally for any user with 3+ check-ins in a week. Rewrote every function to work off check-in presence instead — streaks (already correct), an engagement trend (check-in count first-half vs second-half of window), and three concern types (`silence_streak`, `activity_decline`, `low_engagement`). `analyzeCouplePatterns` rebuilt around per-partner check-in balance — a real "who's drifting" signal, matching what Matt actually asked for. Updated the one live caller (`app/ai-coach/page.js`'s proactive-prompt map) to match the renamed concern types.
  - 📋 Matt's note, not yet scoped: the rebuilt engine still needs a real stress test before it can be trusted — presence/frequency data has "lots of outside forces" (travel, a busy week, one partner's phone dying, app bugs elsewhere) that can look identical to genuine drift/disengagement from the data alone. Worth deliberately generating a range of synthetic check-in histories (steady, declining, bursty, one-partner-only) and confirming `concernFlags`/`driftAlert` fire when they should and stay quiet when they shouldn't, before leaning on this for anything user-facing.
- ✅ Cron diagnostic tooling added, commit b0b3dd9 — Matt asked for a real catalog of cron health after the Weekly Reflection investigation below turned up no code-level bug. `docs/database/cron-runs-diagnostic.sql` (not yet applied by Matt) creates a `cron_runs` table; `/api/cron/scheduled-tasks`'s GET handler now inserts one row per invocation recording which conditional blocks fired, couples processed/errored. **TEMPORARY — explicitly not meant to be permanent.** Matt's instruction: note it clearly so it doesn't outlive its usefulness. Remove once cron health is confirmed over a few weeks: drop the `cron_runs` table and revert the diagnostic block in the GET handler (both are commented `TEMPORARY DIAGNOSTIC` inline for easy grep-and-remove later).
- 🟢 Investigated, not a code bug: the "Vercel Hobby caps cron jobs at 2" theory from July 28 was wrong — Matt's own Vercel dashboard screenshot showed all 7 cron entries registered and Enabled; Hobby's actual constraint is a "flexible 1-hour" firing window, not a job-count cap. Also confirmed `weekly_reflections` already has correct per-week idempotency (`week_start` uniqueness check before generating), so repeated cron firings can't double-generate. Live data showed exactly one missed cycle (Sunday 8/2, no row for week_start 7/27; the prior Sunday 7/26 fired correctly for week_start 7/20) — not the two Matt initially suspected. No root cause found yet since Vercel's log retention had already rolled off by the time this was investigated; the cron_runs table above is what will catch the next occurrence live.
- 🔴 RECURRED — Aug 9/10, Matt: "which didn't fire again this week." Same class of bug as above, now a second occurrence. A likely structural explanation surfaced this pass that the July 28 investigation didn't have: Weekly Reflection gets exactly ONE cron shot per week (`vercel.json`: `"0 13 * * 0"`, gated `if (day === 0)` inside `scheduled-tasks/route.js`) — no redundancy at all, unlike Wednesday/Thursday which get 4+ overlapping windows and were exactly this fragile before their Aug 6 fix. If that single Sunday invocation lands outside Vercel Hobby's flexible firing window, or `getDayInTimezone` doesn't evaluate to 0 at the moment it actually runs, the whole week silently skips with nothing to catch it. Separately: generation itself has a client-side fallback (`weekly-reflection/page.js` calls `/api/reflection/generate` on-demand if `/api/reflection/status` finds nothing), but the push notification telling the couple it's ready does NOT — it only fires inside that one cron invocation, no fallback path. **Not yet confirmed which failed** — need Matt's repro: did the push never arrive (but the reflection was there when you opened the page), or was the reflection itself missing/blank when you opened the page? These have different fixes. Also need to confirm whether `docs/database/cron-runs-diagnostic.sql` (line 199 above) was ever actually applied — if so, this occurrence should be sitting in the `cron_runs` table right now instead of requiring more guesswork.
- ✅ CLOSED, Aug 5 — Follow-Through "never firing" was never a generation bug. Matt got a real Bet Follow-Through the night of Aug 4; root cause of it not appearing live was `FollowThroughCard` only fetching `/api/follow-through/today` once on mount, so a same-session gate-open (his `reveal_seen_at` flipping true mid-visit) was never picked up until a full nav-away/back forced a refetch. Full redesign followed from there — see "Follow-Through delivery redesign — Aug 5" below.

### Follow-Through delivery redesign — Aug 5
Driven entirely by Matt's live use + screenshots the night of Aug 4 into the morning of Aug 5. Went through several iterations; final state below. All commits on `main`.
- ✅ Same-source blend, not a hard swap — added `sourceId` to `/api/follow-through/today`'s response and a `currentSourceId` prop (Bet only, `bet?.id`) so `FollowThroughCard` can tell whether an active Follow-Through belongs to the activity currently on screen. Same-source: appends a quieter `ReportFace` below `children` instead of swapping the slot away, so the Bet reveal stays visible. Different/superseded source (a new day's Bet already generated, still unreported): unchanged standalone swap. Added polling (8s, gated to `currentSourceId` callers only) so the blend surfaces live instead of needing nav-away/back.
- ✅ Shape + cadence fixed after Matt's screenshot showed it reading as a second freestanding card with an instant pop-in — non-wildcard blend now renders inside a shared outer wrapper (matching background, shared radius, `overflow:hidden`, zero gap) so it visually fuses to the bottom of the Bet card instead of leaving a seam. Reveal timing reworked twice: first to a deliberate pause-then-drift (1200ms/900ms) instead of instant, then per Matt's read that the real trigger should be the user sealing their reaction/rating pills (`isSealed` in `BetCard`, surfaced via a new `onSealed` prop) rather than any fixed clock — sealing is now the primary trigger. Fallback for the ~half of users who never tap the (optional) pills: originally a 6s timer, replaced after Matt flagged it could still interrupt someone mid-read (and would show a tall card with unsealed pills above it) — now triggers on returning to the tab after backgrounding it (`visibilitychange`), with a 45s true last-resort timer underneath.
- ✅ Standalone container decoupled from the day's activity card — Matt's catch: the standalone (carryover) report card was keeping the *entire* day's activity card (Bet Tuesday, Notice Wednesday, whichever) mounted and hidden in the DOM the whole time it was unresolved, purely so it had something to flip into. That was the actual source of two rounds of sizing bugs (an invisible-sizer patch was tried first, then superseded). Rewritten as a phase state machine — `resting` (report card alone, self-sized, today's activity not mounted at all) → `entering`/`flipping` (both faces exist only for the ~900ms transition itself) → `settled` (drop the wrapper, render `children` alone). Today's activity card now only ever exists in the DOM for the brief moment the flip is actually happening.
- ✅ Other-directed "waiting" UX fixed — after reporting an other-directed action (something done FOR/TO the partner), the card showed "Waiting on them to check in," which Matt correctly called out as wrong: he already lived that moment with Cass in the real world and has nothing left to wait on. Per the original design spec's own content-vs-synthesis rule, other-directed Tier 2 is a bonus synthesis, not something the reporting partner is missing. Suppressed for `data.mine.directed === 'other'`; still shown for self-directed actions, where the partner's report genuinely is new information.
- 🟢 Design decision made, not yet built: when the standalone carryover container appears (day rolled over, still unreported), it should keep the *same* Follow-Through visual language (same card, eyebrow, buttons) rather than a third distinct style — no attempt to mimic the blended-into-yesterday's-card look, since that context is gone. This is what's currently built; flagging as a confirmed decision, not an open question, in case it resurfaces.
- ✅ Extended to Spark, commit 5b93aa3 — reused Spark's existing `reactionComplete` state as the seal-equivalent trigger (`onSealed` fired from `SparkCard` the same way `BetCard` does off `isSealed`). Zero changes needed to `FollowThroughCard.js` itself — the blend/standalone/phase-machine mechanism was already fully generic once `currentSourceId`/`onSealed` existed as a contract.
- ✅ Extended to Wednesday/Thursday, commit b50a420 — different shape than Bet/Spark: no per-user reveal gate exists for these (`hasSeenSourceReveal` already returns true unconditionally for `wednesday`/`thursday` — cron-driven, both partners see the identical resolved state the instant they open the app, nothing live to protect), so no seal-equivalent trigger was needed — both cards fire `onSealed` immediately once their own entry loads. The real gap: unlike Bet/Spark, `WednesdayCard`/`ThursdayCard` fetch their entry internally rather than living in dashboard page state, so dashboard had no way to know today's entry id for `currentSourceId`. Added an `onSourceLoaded` callback prop to both, fired from a separate effect (not bundled into the fetch effect — `onSealed`'s reference changes once `FollowThroughCard` starts injecting it, which would have caused a redundant refetch every time otherwise, caught before committing). Dashboard captures it into new `noticeSourceId`/`thursdaySourceId` state.
- ✅ Found and fixed in passing, same commit: `WednesdayCard.js` referenced an undeclared `submitError` variable in its late-submission JSX — a real `ReferenceError` crash bug whenever a user reached that specific state (past 7pm, not yet submitted, before the 10pm cutoff). Declared the missing state, wired `handleSend`'s catch to actually set it, added the matching error display to the pre-7pm view too (which never had one).
- ✅ Follow-Through now live on all four daily activities (Bet, Spark, Wednesday/Notice, Thursday). Matt's explicit call, Aug 5: move fast per-activity rather than fully soak-test on Bet alone first, since "each is unique in their own way and we need to be testing and fixing for each." Not yet live-tested through a complete cycle on Spark/Wednesday/Thursday the way Bet has been (one full blend→report→standalone cycle each) — first real signal will come once Matt & Cass use the app normally over the next few days.

### Bug fix — Aug 5 (push notifications)
- ✅ Root cause found and fixed: `registerPushSubscription()` (`lib/push-notifications.js`) looked up "the" existing subscription for a user via `.maybeSingle()` — assuming one row per user — and deleted *every* `push_subscriptions` row for that `user_id` whenever a new device's endpoint didn't match it, before upserting the new one. `/api/push/send` already sends to every subscription on file per user and already prunes a genuinely dead endpoint itself on delivery failure (410/404), so this function never needed to delete anything. Net effect: opening the app on a second device (e.g. laptop) silently wiped the first device's (phone's) registration — matches Matt's report of a missed Wednesday Notice push, and a pattern he'd seen before. Confirmed via `push_log` that the actual send succeeded server-side (status 201) at the time in question — the gap was entirely which device was still registered. Fixed by removing the lookup+delete; now upserts on `(user_id, endpoint)` so multiple devices can hold a live subscription at once.
- ✅ Closed two silent failure points found while investigating (neither explains this specific incident on its own, but both were real gaps): `/api/push/send` returned success-shaped `{sent: 0}` with zero `push_log` row when a user had no subscriptions on file at all; the cron's own `sendPush()` wrapper had a bare `catch {}` swallowing any network/fetch failure with no trace. Both now log.

### Security migrations confirmed applied — Aug 5
- ✅ `docs/database/couples-rls-fix.sql` run by Matt in the Supabase SQL Editor, verified live via `pg_policies` — `couples` SELECT now scoped to `auth.uid() = user1_id OR auth.uid() = user2_id` only (no more code-based read), UPDATE scoped to `auth.uid() = user1_id` with a matching `with_check`. Closes the account-takeover vector from the Aug 4 security sprint.
- ✅ `docs/database/cron-runs-diagnostic.sql` run by Matt in the Supabase SQL Editor — `cron_runs` table now live, `/api/cron/scheduled-tasks` writing a diagnostic row per invocation. Remember this is TEMPORARY (see entry above) — remove once cron health is confirmed over a few weeks.

### Root-cause fix — Aug 5 (assessment data never reaching Nora memory)
Matt's standing priority: any signal that should reach Nora's memory but silently doesn't is high pri. Two layered bugs, both fixed, plus a manual backfill applied for Matt & Cass's real historical data.
- ✅ Bug 1: `SIGNAL_TYPES.ASSESSMENT_COMPLETE` had full signal weights and a full `buildPersonNotesPrompt` lens since the July signal-registry fix sprint, and `seed-memory/route.js` always logged it to `nora_signals` correctly — but it was missing from `INDIVIDUAL_NOTE_SIGNALS`, the routing array that decides whether `updateNoraMemory` actually calls the LLM to synthesize notes. Without it there, a completed assessment logged an event and bumped a counter but never once produced a sentence about that person. Fixed: added to `INDIVIDUAL_NOTE_SIGNALS` in `lib/nora-memory.js`. Matches the routing already documented as intended in `ABF-SIGNAL-REGISTRY.md` (acting user only, not couple-shared) — `couple_notes` intentionally untouched by this signal.
- ✅ Bug 2, bigger: `updateNoraMemory` requires a resolvable `coupleId` as its first step (`nora_memory` is one row per couple — nowhere to write without a couple row). `app/assessment/page.js`'s onboarding path has always saved with `coupleId: null` by explicit design (assessment row needs to exist before a partner is even invited). When that null reached `updateNoraMemory`, the couple lookup failed and the function returned immediately — before ever logging to `nora_signals`. Not misrouted, never recorded at all, no error surfaced anywhere. Fixed with three changes: extracted the assessment→Nora pipeline into a new shared `lib/assessment-memory.js` (`seedAssessmentMemory` / `computeAssessmentResults`); refactored `seed-memory/route.js` to use it (no behavior change to the live path); added a non-blocking backfill into `couples/join/route.js` that fires at pairing time for any pre-pairing completed assessment sitting with `couple_id` null — this protects every couple pairing from here forward, including the 1-3 couples about to start the trial.
- ✅ Verified against production before assuming either bug was the actual cause for Matt & Cass specifically: their `relationship_assessments` rows already had the correct `couple_id` attached (both assessed well after pairing — Cass March 10, Matt June 9), meaning Bug 2's null-coupleId path doesn't explain their gap. Real explanation: both completed their assessment before the July 2 fix that replaced a dead `'PROFILE_UPDATE'` string literal with the real `SIGNAL_TYPES.ASSESSMENT_COMPLETE` constant (see `ABF-SIGNAL-REGISTRY.md` Bug 4) — so even the raw signal was never logged under the right type, and Bug 1 would have blocked notes synthesis regardless if it had been.
- ✅ Manual backfill applied, Aug 5 — `scripts/backfill-assessment-notes.mjs` (rewritten to source from `relationship_assessments.answers` directly rather than `nora_signals`, since the latter had zero rows for either user — sidesteps needing to know which historical bug caused which gap). Run by Matt: `--counts` confirmed both real completed assessments and zero existing `assessment_complete` signals; `--preview` produced notes visibly informed by real assessment data (attachment style, conflict style) woven into existing Nora context, not generic; Matt reviewed and approved; `--apply` ran the real `seedAssessmentMemory()` pipeline for both users — `nora_signals` logged, signal counts incremented, notes synthesized, `user_profiles` scores (re)written, for the first time reflecting real assessment data. Pre-apply `nora_memory` state backed up locally before the write.
- 📋 Not yet done: `relationship_assessments` schema doc (`docs/database/relationship-assessments.sql`) lists a `module_results` column, but every write path (client `page.js`, and now `computeAssessmentResults` server-side reconstruction) treats the shape as `results.modules` — nothing in the app reads the stored column back by name, so this never caused a live bug, but the doc is stale/inaccurate against whatever the live table actually has. Worth a real column check next time anyone touches this table, not urgent now.

### Full audit sprint — Aug 5 (night pass, audit-first per Matt's directive: find and fix real bugs, no new feature scope)
Matt: "Let's audit so we can find and fix bugs without layering in new code." Swept Assessment/Onboarding, AI Coach, Notebook/Practices, Profile/Couples Memory/Timeline. Commits below are all on `main`, pushed by Matt.
- ✅ CRITICAL, commit 691c94a — account deletion left ~35 tables of user data behind. The delete-account route only ever cleaned a handful of tables; every other table with a `user_id`/`couple_id` foreign key (photos, flirts, check-ins, sessions, memory, etc.) silently kept the deleted user's rows forever, including the couple's shared `nora_memory` row surviving a one-sided deletion. Rewritten to enumerate and delete from all ~35 tables in FK-safe order, storage bucket objects included.
- ✅ Two silently-broken partner-notification pushes fixed, matching the standing "signal not reaching where it should" priority:
  - `FlirtSheet.js`'s `sendFlirt()` call to `/api/push/send` had no `Authorization` header at all — every "You've got a flirt 💌" push has 401'd silently since the feature was built. Added `Authorization: Bearer ${session?.access_token}`.
  - `app/us/add/page.js`'s partner-notification push ("a memory was added") sent `process.env.NEXT_PUBLIC_CRON_SECRET` — a var that was never defined anywhere, always `undefined`, always failed auth. Replaced with the real `session.access_token`.
- ✅ BOLA closed in `/api/push/send` — any authenticated (non-cron) caller could target any `userId` and push arbitrary notification content to them. Restricted non-cron callers to self-or-partner via a couple-membership check; cron-authenticated calls (Wednesday Notice, etc.) remain unrestricted since those are legitimately system-wide.
- ✅ Dashboard's empty-state "Add a memory →" button was routing into the superseded `/timeline` page instead of `/us/add` — repointed. (`/timeline` + `AddEventModal`/`EventDetailModal` are now fully unlinked from the app; see flag below.)
- ✅ Four `lib/nora-memory.js` signal-routing gaps closed — same bug shape each time: full signal weight + a written prompt lens, but missing from the array that actually gates whether notes synthesis fires, so the signal logged and incremented a counter but never produced a sentence about the person/couple:
  - `FLIRT_RECEIVED` — added to `INDIVIDUAL_NOTE_SIGNALS`.
  - `PRACTICE_ADDED`, `PRACTICE_UPDATED`, `SHARED_ITEM_COMPLETED` — added weights + routing.
  - `MEMORY_REFLECTION` — added to `COUPLE_SIGNAL_WEIGHTS` + `SHARED_SIGNALS` (couple-level, not individual).
  - New signal type added: `FLIRT_PROFILE_BUILT` (weights + `INDIVIDUAL_NOTE_SIGNALS` + a new prompt lens). `app/api/flirts/save-profile/route.js` had been mislabeling this event as `SIGNAL_TYPES.FLIRT_SENT` — a real flirt-profile build (humor style, flirt style, chosen media) was being folded into Nora's "sent a flirt" bucket instead of getting its own signal. Corrected to the new type.
- ✅ Dead-code cleanup — 5 files confirmed dead (zero importers across `.js`/`.mjs`/`.jsx`, verified twice after an internal false-positive check) and neutered via deprecation-notice stub (sandbox `rm` is blocked; each stub gives Matt the exact `rm` command): `lib/checkin-questions.js`, `lib/spark-questions.json`, `components/ReflectionCard.js`, `lib/date-utils.js`, `app/api/game-room/challenge/start/route.js` (this last one had already gotten a real auth fix earlier in the engagement despite being unused — confirmed orphaned, not a live security hole, before neutering).
- ✅ Lint cleanup, 13 real `react/no-unescaped-entities` violations across `app/dashboard/page.js` (5), `app/ritual/page.js` (3), `app/onboarding/page.js` (2), `components/RitualCard.js` (4 apostrophes across 3 spots) — fixed manually (sandbox's `next lint`/`eslint` both non-functional this session, unrelated environment flakiness) via `&apos;`. `app/connect/page.js` checked, already clean.
- 📋 Legacy system flagged, not removed: `app/timeline/page.js` + `components/AddEventModal.js` + `components/EventDetailModal.js` — a self-contained older "Been"/timeline implementation, now fully unlinked (its one entry point was the dashboard button just repointed above) since `/us` + `/us/add` do the same job better. Old design language, separate storage bucket (`timeline-photos` vs. the app's `photos`), and a weaker delete path (`EventDetailModal`'s `handleDelete()` has no `created_by` check, unlike `/us`'s server-enforced creator-only delete). Worth a real retire-or-keep decision rather than leaving it live-but-unlinked.
- 📋 Confirmed dead but deliberately not removed, single functions inside otherwise-live files (lower value/risk than the whole-file removals above — flagged for Matt's keep/wire-in/delete call): `lib/date-suggestions.js`'s `CATEGORY_CONFIG` and `fetchNearbyPlaces`; `lib/dates.js`'s `getDayLabel`; `lib/giphy.js`'s `getTrendingGifs`/`getRandomGif`; `lib/ritual-suggestions.js`'s `getRitualsByTag`/`getRitualsByTier`/`getSeasonalRituals`.
- 📋 Two "forgotten useful function" finds, per Matt's ask to flag anything dead that might still be a good idea for the current app: `lib/challenge-prompts.js`'s `MEMORY_UNLOCK_COPY` (fully drafted push copy for Memory Test unlocking, never wired into any actual send path); and, distinct from the July-sprint-rebuilt `analyzeUserPatterns` (which does have a live caller in `ai-coach/page.js`), `checkin-patterns.js`'s couple-level `analyzeCouplePatterns()` specifically has no live caller anywhere — a real engagement-balance/drift-between-partners detector sitting unused.

### Post-audit fix pass — Aug 5 evening / Aug 6 (Matt's decisions on the audit's open items, plus live bugs he hit)
Matt reviewed the audit findings above and made the calls; this section is that work plus two live bug reports.
- ✅ Killed outright, commit 16c79b0: `lib/date-suggestions.js`'s `CATEGORY_CONFIG`/`fetchNearbyPlaces`, `lib/dates.js`'s `getDayLabel`, `lib/giphy.js`'s `getTrendingGifs`/`getRandomGif`, `lib/ritual-suggestions.js`'s `getRitualsByTag`/`getRitualsByTier`/`getSeasonalRituals` — Matt's call: no evidence anything needed them, not worth keeping as clutter.
- ✅ `/timeline` + `AddEventModal`/`EventDetailModal` fully retired, commit 6238b0b (run locally by Matt after the sandbox's git repeatedly bus-error-crashed on this specific commit) — confirmed via repo-wide grep that nothing linked to `/timeline` anymore before retiring. `page.js` kept as a `router.replace('/us?section=been')` redirect rather than a comment stub since it's a Next.js route file and needs a valid default export to not break the build; the two modal components are plain components (not part of the routing tree) so comment-only stubs were safe.
- ✅ Memory Test unlock notification wired in, commit a65d589 — `MEMORY_UNLOCK_COPY` was drafted during the original build but never sent; the eligibility gate (`checkMemoryUnlocked`) and server-side enforcement were already fully live, the couple just had no way to find out they'd crossed the threshold except opening Game Room and seeing the tile flip. Added `notifyIfMemoryJustUnlocked()` (check-on-write, fires from `timeline/event`, `spark/respond`, `bet/respond` — the three routes that affect the unlock inputs), gated on a new `couples.memory_unlock_notified_at` column (migration `docs/database/memory-unlock-notified.sql`, confirmed applied by Matt) so it only fires once. Known gap, accepted for v1: a couple already past the count thresholds and only waiting on account age won't be notified until their next timeline/Spark/Bet write — no time-based sweep.
- ✅ `ENGAGEMENT_PATTERN` checkin-drift signal wired into Nora's `couple_notes`, commit 79b8911 — Matt's explicit concern: presence-only check-in data can't distinguish genuine drift from an ordinary busy week/dead phone/travel, and stating that as fact would cost trust. Built accordingly: `analyzeCouplePatterns` now requires the SAME partner to be less-active across the two most recent complete weeks independently (not a single-window aggregate) before firing at all; the resulting signal only ever touches `couple_notes` (never either partner's individual notes) and is excluded from both signal-count weight dicts (it's Nora noticing an absence of action, not a couple taking one); its prompt lens explicitly instructs treating it as the weakest tier of evidence, never writing words like "drifting," and at most shaping a warmer open-ended question rather than a pointed one. Runs weekly via cron alongside Weekly Reflection. `scripts/test-checkin-patterns.mjs` stress-tests it against 7 synthetic histories (steady, bursty, single-bad-week, genuine 2-week drift, flip-flop, sparse, empty) — all 7 pass.
- ✅ CRITICAL, commit 9d19e0a — Wednesday Notice losing Nora commentary + Follow-Through on late-but-before-cutoff submissions. Matt's live repro: both partners submitted after the 7pm deadline but before the 10pm cutoff; both notices were captured and shown, but no Nora synthesis and no Follow-Through ever appeared. Root cause: `processWednesdayReveal` (the 7pm cron) unconditionally stamped the entry `status: 'revealed'` even with zero or one submission in hand — and `processWednesdayCutoff` (the 10pm catch-all) only processes entries still `status: 'pending'`, so the 7pm cron permanently blinded the 10pm pass before the couple had even finished submitting. Fixed: the 7pm cron now only proceeds if BOTH partners have already submitted (a true early reveal for couples done on time); otherwise it leaves the entry `pending` so the 10pm cutoff's already-correct partial/empty/full handling gets the one real reveal pass instead. Closes both the both-late case Matt hit and the adjacent one-early/one-late case.
- ✅ Same bug class found and fixed in Thursday, commit 6238b0b, different shape — Thursday has no explicit deadline shown anywhere in its UI (unlike Wednesday's 7pm/10pm) and `thursday/respond` never checked entry status before writing, so a response submitted any time after `processThursdayReveal`'s single ~6-7pm cron pass got captured but never synthesized (no second cron to catch it, unlike Wednesday). Fixed by mirroring how Bet/Spark already work — `thursday/respond` now triggers synthesis + Follow-Through immediately at write-time, the moment a response completes the pair, instead of depending on any cron window at all. Idempotency guarded on `entry.status === 'pending'` (checked before and during the update). `processThursdayReveal` cron is unchanged — now purely the partial/nobody-responded evening fallback.
- ✅ 4 Flirt postcard bugs fixed, commit 04da79d, from Matt's live use ("it's so good but broken"):
  - No way to browse received flirts once everything's been opened — `view === 'stack'` (the received reader) was only reachable via the "open flirt" CTA, gated on `hasUnseen`. Added a persistent "received →" list view mirroring the existing "sent →" one.
  - Received-view stamp SVG was `position: absolute` overlapping the To/Name text in the same 82px column (Matt: "the stamp is still in the wrong spot") — moved to normal flow above the name so they can't collide regardless of name length.
  - Photo upload had zero error handling and no loading state — a failed/slow upload left the button looking unchanged with no preview and no explanation. Added a real handler with try/catch, an "uploading..." state, a visible error + retry, and a proper file extension in the storage path.
  - Sent list rendered literal text "Photo"/"GIF" instead of a thumbnail for those types — added real thumbnails to both the sent list and the new received list.
- ✅ Memory Test "something went wrong" root-caused and fixed, commit ba01ad9 — recurring, Memory-specific only (never reported on Rank/Story/Pitch, which share the same `challenge/generate` endpoint). Root cause: Memory's expected JSON has 5 free-text fields (question, answer, 3 hints) vs. 1-2 for every other type, at the same shared 600-token output budget — far less margin before a verbose response gets cut off mid-JSON, `JSON.parse` throws, generate 500s, client's bare `catch {}` collapses it to the same generic message every time. Fixed: memory gets a higher token budget (1100 vs 600); JSON parsing now extracts the first `{...}` block instead of assuming the whole trimmed response parses cleanly (robust to stray preamble on complex prompts); the 3 bare `catch {}` blocks in `challenge/play/page.js` now log the real error so any future recurrence is actually diagnosable.
- ✅ 3 more Flirt fixes from Matt's follow-up screenshot, commits 4cd77db + 1aa20ce: (1) photo upload was still failing after the previous error-handling fix — root cause was `handlePhotoUpload` minting a brand-new unauthenticated Supabase client instead of reusing the file's existing session-aware one, so every upload hit Storage RLS as anonymous and got rejected; (2) stamp still not top-right — the SONG/WORD/PHOTO/GIF/MEMORY selector row spanned the full card width above the content/address split, structurally blocking the address column from ever starting higher (Matt diagnosed this correctly himself); moved the selector inside the content column so the address/stamp column starts flush with the header; (3) polish — photo preview bumped from 70px to 160px max height, stamp pinned to the column's right edge (was stretching full-width by flexbox default with the image left-aligned inside that box).
- ✅ Couples Nora Session — **foundation only**, commit 54772ea, no UI/entry-point yet (deliberately separate next phase). Confirmed with Matt this is a true third Nora mode: live, both partners present at once — distinct from async Wednesday/Thursday (separate answers, synthesized after) and private 1:1 AI Coach. Verified the discrete-session pattern (title/close/history, replacing an infinite thread) was already built and proven for solo AI Coach (`ai_conversations`/`ai_messages`, close-session route) but never ported to a shared/couple context — `nora_inline_sessions` (NoraCouplesChat) still just grows one row forever with no boundary. Ported the proven solo pattern to `type='shared'` (already supported in `ai_conversations`'s CHECK constraint + RLS, unused until now) via new `app/api/couples-session/route.js` + `close-session/route.js`, couple-scoped throughout (either partner can continue/close a session the other started). Added `ai_messages.sender_id` (migration: `docs/database/ai_messages_sender_id.sql`, Matt to run) so history can be correctly labeled by speaker — nora-inline's pattern strips this, so today Nora can't actually track who-said-what turn over turn; fixed for the new surface since "track the dynamic between them" is the whole point. New `SIGNAL_TYPES.COUPLES_SESSION` in `lib/nora-memory.js`, deliberately NOT routed like `NORA_CONVERSATION` (private-notes-only, correct for solo AI Coach) — routed like a witnessed shared activity instead (both partners' individual notes + couple_notes update), since nothing said with both partners present is private from either of them. Also deliberately omits `getFullNoraContext`'s claims-surfacing block from the system prompt — surfacing a hypothesis inferred from one partner's private signals in front of the other would reopen the exact privacy leak the Aug 2026 AI-coach fix closed.
- ✅ Couples Session page built full, commit 8f2bdcf — live shared chat + history, three-way speaker-labeled bubbles (Nora/me/partner), lightweight 8s poll while a session is open. Reachable at `/couples-session`, no entry point wired anywhere yet at this point.
- ✅ Sunday Weekly Reflection hook wired into Couples Session, commit aa7db33 — replaced the inline `NoraCouplesChat` widget on `/weekly-reflection` with a Couples Session invite card, seeded via a new `?seed=` param (mirrors `ai-coach`'s pattern) so the session opens already grounded in that week's actual reflection pattern instead of starting cold. First real, live entry point into Couples Session.
- ✅ Dashboard hero-slot promotion rotation shipped, commit 249d413. Full feature inventory done first: confirmed solo AI Coach has zero proactive nudge anywhere despite being a bottom-nav tab (purely nav-discoverable), and Memory Test's own code (`lib/memory-unlock.js`) already documents an accepted gap — the unlock push fires once, then no reminder ever again for an eligible-but-unplayed couple. v1 candidate pool confirmed with Matt: Couples Session, AI Coach, Memory Test, Date Night. Mixtape and Notebook/Practices identified as real gaps too but held for a later pass. Read `app/api/dashboard/hero/route.js` in full before touching it — key finding: every message is freshly LLM-generated per call (`noraChat`/`noraSignal`), never fixed copy, so this could not just swap in static promo strings. Instead, added a new PART 5b block that only fires on the existing priority-5 "quiet slot" (weekends, Friday with no ritual, no upcoming date within 3 days) and only for users with established memory (never touches the crafted first-session `isNewUser` flow). Per-candidate eligibility: Couples Session (skipped entirely on Sunday — the Weekly Reflection hook already owns that day — + no shared `ai_conversations` activity in 5 days), AI Coach (no solo `ai_conversations` activity in 4 days), Memory Test (`checkMemoryUnlocked()` true + no memory `challenge_sessions` attempt in 7 days), Date Night (no upcoming `custom_dates` + none completed in 14 days). Anti-repeat + frequency cap tracked via a new nullable `hero_cache.promo_type` column (migration `docs/database/hero_cache_promo_type.sql`, Matt to run) — won't repeat the same promo within 5 days, caps total promo appearances at 2 per 7-day window per user; all four windows/the cap are tunable defaults, not locked numbers. When a candidate is selected, the mechanism injects a short prompt addendum instructing Nora to weave in a brief, in-voice second sentence after her usual observation (never a feature announcement) and overrides `cta_label`/`cta_href` to point at the chosen feature.
- 🔴 Two more gaps surfaced during the inventory, Aug 10 — Matt: "We don't even have Mixtape show up anywhere in the app," and separately, no birthday/anniversary surfacing fired for his birthday (Saturday):
  - Confirmed via grep: `app/mixtape/page.js` has **zero** references anywhere else in the codebase — no nav link, no dashboard card, no button pointing to it from any other file. A fully built, fully orphaned page with no discovery path at all, not even a buried one.
  - Confirmed: `user_profiles.birthday` (and the "important dates" modal's `anniversary`/`met`/`firstDate`/`firstKiss`, written to `timeline_events`) are **passive only**. `birthday` is fed into `lib/nora-knowledge.js`'s AI Coach system-prompt context — useful only if the user happens to already be mid-conversation with Nora in `/ai-coach` and it comes up. Nothing anywhere (no cron, no push, no dashboard hero branch) actively checks "is today/this week someone's birthday or the couple's anniversary" and does anything about it. The dashboard hero route's only date-awareness branch (`priority === 5 && nextDate && daysUntilDate <= 3`) is exclusively about scheduled `custom_dates` (Date Night plans), not birthdays/anniversaries at all. Matt's memory of this being "wired in" appears to be the passive-context piece only — an active surfacing feature was never actually built. Given a birthday is a single-day window, this deserves a real fix (push notification + a hard pre-empt tier in the hero route, same shape as the Follow-Through/Ritual nudges), not just a rotation-pool candidate — scoping as its own item, separate from the discovery-promo rotation.
- ✅ Active birthday/anniversary surfacing built with lead time, commit 93e185e. Matt's explicit design note before building: day-of surfacing gives no lead time to actually plan/buy/book anything — confirmed 7-day heads-up + 2-day closer reminder, both push and dashboard (existing Date Night 3-day "coming up" lead on already-scheduled `custom_dates` left as-is, that's a different problem — surfacing a plan already made, not prompting one). Added `daysUntilNextOccurrence(dateStr, timezone)` to `lib/dates.js` (ignores stored year, matches month+day, wraps to next year, verified against 7/2/0/wrap-around cases) so both surfaces agree on the same math. Push side: new `processBirthdayAnniversaryReminders` in the cron, gated on the same once-daily 3am local hour as `processDailyContent`, fires at exactly 7 and 2 days out (exact-day match only trips once/year, no dedupe column needed). Dashboard side: new PART 0c in `dashboard/hero/route.js`, same hard-pre-empt shape as Follow-Through/Ritual (PART 0/0b) — bypasses the daily cache, shows for the whole 0-2 day window so it's not missed if the exact push day is skipped. Birthday surfaces to the PARTNER only, never the person whose birthday it is; anniversary surfaces to both. Known simplification: no check against whether a `custom_dates` entry already exists for the event (no link between the two data models) — a couple who's already planned something still sees the reminder for up to 3 days, accepted as low-cost against the app's existing bias toward over- rather than under-surfacing.
- ✅ Mixtape reviewed, evaluated, and fixed end to end, commit 15625c4. Matt asked for a code review + evaluation + entry-point decision before investing further. The review surfaced a real bug underneath the styling problem, not just a missing entry point: **songs were never actually reaching Mixtape.** Flirt's song send path had been refactored at some point to store song data in a single `metadata` JSONB column (`app/api/flirts/send/route.js`), but Mixtape was still querying/reading a separate, older set of flat columns (`spotify_track_id`, `spotify_track_name`, etc — added by `docs/database/song_flirts.sql` during an earlier version of the send flow) that nothing had written to since. Every song sent since that refactor was invisible to Mixtape despite rendering correctly inside Flirt itself — also found the Spotify search result's track id was captured in the UI but never even included in the send payload, so the flat `spotify_track_id` column could never have been populated regardless. Fixed the write path (`FlirtCard.js` now includes `track_id`; `flirts/send/route.js` now mirrors song metadata into the flat columns), and — since `spotify_track_id` was never historically captured and can't be backfilled — relaxed Mixtape's eligibility filter to key off `spotify_track_name` instead (present on every real song send since day one), so the backfill migration (`docs/database/mixtape_data_flow_fix.sql`, Matt to run) restores full song history, not just future sends. Also, per Matt's review answers: fixed both of Mixtape's internal links (pointed at a nonexistent `/flirts` — Flirt is the `FlirtCard` component on `/dashboard`, not its own page), fully restyled from the leftover dark Spotify-green look (pre-dates the app's design refresh) to the current Cormorant Garamond/DM Sans cream palette matching `/dates`, cut the "Premium Features Coming Soon" banner (nothing behind it was built or scheduled), and added a "mixtape →" link to `FlirtCard`'s postcard-back view next to the existing "received →"/"sent →" links — bumped all three from `fontSize:8` to 12 with real tap padding, since Matt flagged them as "very small and hard to see." `components/SongFlirtCard.js` (confirmed zero references anywhere) is dead code but the sandbox mount blocks file deletion the same way it blocks git's own lock files (`unlink` fails, `mv`/rename works) — left in place for Matt to delete manually.

---

## NORA STANDALONE

### App Store
- 🔴 Apple Developer account — $99/year enrollment at developer.apple.com/programs/enroll. Blocks device signing and submission. Everything else is ready.
- 🟡 App Store assets — screenshots, description, keywords. Not yet created.

### Monetization
- 🟡 Freemium model — 30-day reverse trial (unlimited), then 100 msg/month free, $7.99/month premium. Designed in detail, not built. Requires RevenueCat + StoreKit 2 integration.

### Infrastructure
- 🟢 Nora email domain — privacy policy uses coggan11@gmail.com. Needs proper domain before public launch.
- 🟢 Welcome email sender — currently onboarding@resend.dev. Needs branded sender.
- 🟢 nora_conversations table — safe to deprecate 30 days after migration to nora_sessions (migrated July 2026).

---

## NORA FOR PRACTITIONERS

- 🟡 Constitution V1 — drafted
- 🟡 Research — Tally form live at tally.so/r/aQgao2, two practitioners contacted
- 📋 Product build — waiting on practitioner research synthesis before scoping
- 📋 Practitioner dashboard — therapist sets frame, Nora holds between-session space

---

## BOTH PRODUCTS

### Voice + Quality
- 🟢 Nora voice refinement — reduce response restatement, vary entry points, cut affirmation formula before substance, closing questions should open new territory not summarize. System prompt pass needed on BOTH products.

### UX
- 📋 Pinch to zoom in photo viewer — blocked by PWA viewport restrictions. Log for native app era.

---

## COMPLETED (recent)

- ✅ FlirtCard redesign — retro postcard front, 3D flip, airmail back, ABF stamp, tap reactions
- ✅ Memory / Photo feature — /us/add, partner photo append, Nora observation, photo gallery viewer
- ✅ Notice late submission — extended to 10pm Pacific, 6pm evening reminder push
- ✅ Date Night — color palette, custom_dates consolidation, creator auto-confirm, mark done guard, conversation starters fixed, Next Up hero photo
- ✅ Security audit — Next.js 16.2.10, ANTHROPIC_API_KEY fix, RLS verified, rate limiting
- ✅ Nora Standalone — iOS simulator verified, new user experience, push notifications, privacy/terms
- ✅ July 28 — Date Night bug batch: grid ordering, hero image locking, dead List/Map toggle removed, Movie/Show map fix, planning-flow redesign, multi-photo upload, dashboard hero CTA routing fix
- ✅ July 28 — Full palette pass across Date Night (2 rounds — CURATED_IDEAS cards, history header, Next Up banner, button gradients), km→miles distance fix
- ✅ July 28 — Post-date reflection mechanic (reaction pills, no stars/emoji, Mark as Done per-user guard + partner push, Nora observation, morning-after cron nudge)
- ✅ July 28 — Ideas For You Two personalization (assessment-weighted category picking, avoidPlaceIds, Ticketmaster keyword variation), fixed two follow-on bugs (Slow/Just Us collapsing onto the same category by default; maxPrice/price_level never actually applied)
- ✅ July 28 — Code audit: missing force-dynamic on 1 route, 4 leftover debug console.logs in profile photo upload, missing cron schedule entry for the morning-after push, Date History star badge silently going blank for new completions
